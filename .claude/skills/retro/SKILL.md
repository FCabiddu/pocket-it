## Launcher

Spawn the native agent. Call the Agent tool immediately with:
- `subagent_type`: `retro`
- `description`: `Retro — turn repeated review findings into rules`
- `run_in_background`: `false`
- `prompt`: exactly the text below

---

$ARGUMENTS
