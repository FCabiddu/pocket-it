## Fatti
- Correzione all'affermazione 'prima riga BUDGET mai registrata' nel commit f99be38 e nel messaggio di sessione: e' falsa. Misurato dopo, con 'handoff.sh recent --all | grep -c BUDGET' = 4 righe, tre delle quali precedenti a PI-16 e in due unita' diverse. L'affermazione veniva da un conteggio di un altro report, non da un comando eseguito: e' esattamente il difetto che implementing-common.md §9 vieta, commesso dall'orchestratore. Un messaggio di commit su main e' permanente: l'affermazione che un comando puo' decidere si esegue prima di scriverla, anche nel diario.
