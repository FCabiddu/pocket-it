## Fatti
- bin/handoff.test.sh:1150 (AC8 'reviewer.md is untouched by this task') compares the WORKING TREE against origin/main with a two-dot git diff, so a branch merely BEHIND its base goes red for a file it never touched — measured on PI-48/PR #112: branch diff of .claude/agents/reviewer.md vs merge-base 68c1511 empty, main's #113 (790d315) edited it, branch 364 ok/1 FAIL while origin/main was 365 ok/0 FAIL; green again after merging main in. A three-dot (BASEREF...HEAD) or merge-base comparison is the fix. Also: verify.sh on this repo selects only the *.test.sh files in the diff, so it cannot see this at all (PR #112: 'verify: GREEN' with the chain red).
