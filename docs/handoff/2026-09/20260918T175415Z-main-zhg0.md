## Log
- 2026-09-18 PI-62 reviewer: race explanation for duplicate _classify_base confirmed by mechanism — bin/doctor.test.sh mutates the real bin/doctor.sh in place (SCRIPT=pwd/doctor.sh, not a sandbox copy), restore is reliable only for the test's own exit, no protection against a concurrent external reader/writer — pre-existing since 780a5ee8 (2026-09-17), not introduced by PI-62
