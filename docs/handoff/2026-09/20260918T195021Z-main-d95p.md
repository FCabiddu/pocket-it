## Log
- 2026-09-18 PI-63 PR #108 merged (e997e07) — doctor.test.sh recursion mirrors a copy, never mutates the checked-out bin/doctor.sh; AC4 SIGINT case fixed with set -m + kill -INT on the process group + completion marker
