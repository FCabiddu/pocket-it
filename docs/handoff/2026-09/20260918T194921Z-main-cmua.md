## Log
- 2026-09-18 PI-63 PR #108 approved (delta 1) — merge: orchestrator — cause: other: mirror_run returns rm -rf's status, so the AC4 control's marker is written even when its case never ran (non-blocking, fix at a follow-up on bin/doctor.test.sh)
