# PI-14 — Compositore in sola lettura per la memoria della pipeline

## Cosa cambia e perché

`bin/handoff.sh` guadagna quattro comandi puri (`facts`, `show`, `recent N`/`recent --all`, `grep REGEX`)
che compongono, a ogni invocazione, le sorgenti congelate (`docs/SESSION_HANDOFF.md`,
`docs/SESSION_HANDOFF_ARCHIVE.md`, entrambe opzionali) con eventuali frammenti sotto `docs/handoff/**`
(formato TAD §4.2 — non ancora scritti da nessuno, arrivano con PI-16) e i loro compattati sotto
`docs/handoff/archive/**` (`### {nome}` per sezione, PI-18). Nessuno dei quattro comandi scrive: niente
`mkdir`, niente creazione del file, niente `sed -i` di normalizzazione (ADR-2 del TAD). `log` e `fact`
restano esattamente quelli di oggi — stesso formato, stessi exit code, stesso messaggio — e sono l'unica
parte dello script che ancora scrive, solo nel vecchio file.

Ristrutturazione minima nello script: i quattro comandi di lettura sono intercettati **prima** del blocco
che crea/normalizza `docs/SESSION_HANDOFF.md`, così quel blocco (mkdir, heredoc di creazione, `sed -i`)
resta raggiungibile solo da `log`/`fact` (e dal ramo `*)` di comando non riconosciuto, comportamento
preesistente e invariato).

## Criteri di accettazione → test

Tutti in `bin/handoff.test.sh`, sezione "PI-14: read-only composer":

- **AC1** — `facts` byte-identico all'`awk` di oggi, sulla copia reale di `docs/SESSION_HANDOFF.md` di
  questo repo (29 fatti). Test: "AC1 — facts byte-identical to the pre-PI-14 awk…".
- **AC2** — `recent --all`, con l'archivio reale aggiunto, multinsieme uguale a Log+archivio (118 righe
  attese, 118 ottenute — contato nel test). Test: "AC2 — recent --all multiset equals Log + archive".
- **AC3** — frammenti di prova: riga duplicata in due frammenti (appare due volte), sezione d'archivio con
  lo stesso nome di un frammento (copia dichiarata, contata una volta), fatto ritirato (non appare), stesso
  fatto riscritto dopo il ritiro (torna visibile). 5 test dedicati.
- **AC4** — `facts`/`show`/`recent 5`/`grep X` non scrivono: `git status --porcelain` identico
  prima/dopo, verificato **due volte** (repo senza il vecchio file, e repo con il vecchio file già
  committato), più l'assenza di `docs/handoff/` creato da una lettura. 4 test.
- **AC5** — riga congelata 2026-09-10, frammenti 2026-09-11 e 2026-09-12: `recent --all` ordina
  12, 11, 10. Test: "AC5 — order is 12, 11, 10".

Due test preesistenti (PI-8) usavano `show` per innescare la creazione/normalizzazione del file — comportamento
esplicitamente tolto da ADR-2. Aggiornati per usare `log`/`fact` (scritture vere) come innesco, non
`show`: "AC4 — stale 'max 30 righe' comment is normalised on the next write" e "PI-8 F2a — a freshly
created file's Log header shows the live cap". Nessun altro test preesistente toccato.

## Mutazioni (TAD §11.3) — rosso prima, verde dopo

- **AC1**: regex di estrazione sezione alterata per non matchare mai → rosso su "AC1 — facts
  byte-identical…"; ripristinata → verde. Solo quel test cade, gli altri restano verdi (buona
  localizzazione).
- **AC3 (ritiro)**: tolta la condizione "sorgente più vecchia del ritiro" dal confronto hash → rosso su
  "AC3 — the same fact rewritten after the retract is visible again" (un fatto riscritto nella stessa
  fonte del ritiro torna nascosto per sempre, sbagliato); ripristinata → verde.
- **AC4 (no-write)**: iniettata una scrittura deliberata in `facts` → rosso su "AC4 [withold] — git
  status --porcelain identical before/after four reads" (la variante "bare" non lo cattura perché la
  scrittura di prova falliva prima ancora di toccare disco, in assenza di `docs/`: nessuna debolezza del
  test, solo della mutazione scelta); ripristinata → verde.

## Porte di qualità

- `bash bin/handoff.test.sh` → `all ok` (tutti i casi, vecchi e nuovi).
- Tutto il `testCommand` di progetto (10 suite: cleanup-merged, worktree, doctor, handoff, next-wave,
  guard, guard.heredoc, verify, install-live, install-live.readme) → tutte exit 0.
- `shellcheck -x bin/handoff.sh` → nessun avviso.
- `bash -n bin/handoff.sh` → sintassi ok.

## Deviazioni

Nessuna dal TAD. Due test PI-8 preesistenti aggiornati per lo stesso motivo dichiarato in ADR-2 (elencati
sopra) — è una correzione richiesta dal cambiamento di comportamento voluto da questo task, non una
scelta indipendente.

## File toccati

- `bin/handoff.sh` — compositore (`compose()`, invocato da `facts`/`show`/`recent`/`grep`); riordino del
  blocco di creazione/normalizzazione perché resti raggiungibile solo dalle scritture.
- `bin/handoff.test.sh` — 17 nuovi test PI-14, 2 test PI-8 aggiornati per non usare più `show` come
  innesco di scrittura.

## Turni e budget

Ripreso dopo un'interruzione per limite di spesa (nessun lavoro precedente committato: worktree creata da
zero in questa sessione). Entro il budget di 200 assegnato al task; nessun `BUDGET`/`STALL` da loggare.

## Giro 2 — review NEEDS WORK, chiuso

`git pull --ff-only` sui due commit del reviewer (`1dc9a06` risoluzione conflitto con `main`/PI-31,
`a58ba07` stato + log). Findings dal commento
[#issuecomment-5655671786](https://github.com/FCabiddu/pocket-it/pull/63#issuecomment-5655671786):

1. **Mutazioni rimaste verdi (6) → chiuso.** Per ognuna: mutazione rifatta identica a quella del
   reviewer, verificata rossa, poi verde dopo il fix. Nuovi test in `bin/handoff.test.sh`:
   - **AC5, seconda metà (M1/M4/M5).** Fixture con lo stesso giorno su main-log, archivio (2 righe) e
     due frammenti a timestamp diversi: "AC5 — same-day tie-break…". M1 (log congelato prima dei
     frammenti), M4 (archivio letto dall'alto), M5 (frammenti in ordine crescente) — tutte e tre
     catturate dallo stesso test.
   - **AC3(a) — archivio compattato mai letto (M2).** Sezione `### orphan` senza frammento vivo
     corrispondente: "an archive section with no live fragment (orphan) is still read". Mutazione
     `if os.path.isdir(ARCHIVE_DIR)` → `if False` catturata.
   - **AC3(b) — il ritiro nascondeva solo i fatti congelati (M3).** Fatto in un frammento più vecchio
     del ritiro, stesso ritiro che nasconde anche un fatto congelato: "the retracted fact (fragment
     source, older than the retract) does not appear". Mutazione che aggiunge `and s["age"] == ""` al
     confronto catturata.
   - **AC3(c) — dedup dei fatti per testo, mai provata da sola.** Stesso testo in due sorgenti (frozen +
     frammento): "the same fact in two sources counts once".
   - **AC4 — `sed -i` di normalizzazione reintrodotto nelle letture, invisibile (M6).** La variante
     `withold` copiava la memoria reale di questo repo, già `max 100 righe`: un `sed` riapplicato è un
     no-op, `git status` resta pulito. Nuova variante `stale-comment+fragments`: file committato con
     `max 30 righe` + un frammento + un'archivio, il `sed` reintrodotto produce un diff vero, catturato.
     Aggiunto anche il caso fuori da un repo (exit 1, nessun file).
2. **Righe troncate (`bin/handoff.sh:53`) → chiuso.** `dash_lines` usava `body.splitlines()`, che spezza
   su CRLF, CR solo, VT, FF, FS, GS, RS, NEL, LS, PS — separatori che `awk` (RS="\n") ignora come dati.
   Fix: `read()` apre con `newline=''` (disabilita la traduzione universale-newline, così CRLF/CR restano
   byte letterali) e `dash_lines` divide solo su `'\n'`. 10 nuovi test (uno per separatore, `Finding4
   [SEPNAME]`), ciascuno verificato rosso con `splitlines()` reintrodotto, poi verde.
3. **Codici d'uscita → chiuso.** `recent abc`, `recent --al`, `grep '['` uscivano con 1 e un traceback;
   `recent -1` con 0 mostrando tutte le righe meno l'ultima (slice negativo silenzioso); `grep` senza
   argomento con 2 ma senza riga d'uso. Fix: `usage_exit()` (stderr + exit 2) per `recent` con `N` non
   `^[0-9]+$` (rifiuta non-numerico, `--al`, e i negativi) e per `grep` con regex non compilabile
   (`try/except re.error`) o senza argomento. 5 nuovi test, uno per forma malformata, tutti verificano
   sia l'exit code sia la riga `usage:` — mutazione che toglie la validazione verificata rossa.
4. **Fatto diventato falso → chiuso.** `docs/SESSION_HANDOFF.md`: «normalizzazione on every subcommand
   run» non è più vera (solo `log`/`fact` normalizzano). Corretto con `bash bin/handoff.sh fact "…"` di
   questo branch (nessuna riga toccata a mano), come da convenzione in `implementing-common.md` §3 —
   il vecchio fatto resta (nessun `retract`, arriva con PI-16) ma è superato dal nuovo.
5. **Altri punti del commento.** Nessun altro finding bloccante elencato oltre i 4 numerati e i codici
   d'uscita; la nota non bloccante su un comando sconosciuto (mkdir prima dell'usage) è preesistente e
   fuori scope per questo task (il TAD la mette in conto solo fuori da un repo).

**Porte di qualità, giro 2:** `bash bin/handoff.test.sh` → 94 `ok`, nessun FAIL. Tutto il `testCommand`
(11 suite incluso il nuovo `status.test.sh` di PI-31) → tutte exit 0. `shellcheck -x bin/handoff.sh` →
pulito. Tutte le 9 mutazioni del giro 2 (6 del reviewer + splitlines + validazione argomenti) verificate
rosse prima del fix e verdi dopo.
