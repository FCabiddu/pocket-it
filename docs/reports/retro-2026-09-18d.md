# Retrospective — signals-2026-09-18d

**Scope**: 1 `retro-due.sh` signal (PI-63 / PR #108, a cause named inside an approval) plus 5 mechanism observations handed over with it, all of 2026-09-18. 1 PR read, 2 rounds (first-round NEEDS WORK → delta 1 APPROVED), rework rate **1/1**. Facts **76 → 75**. Lessons **10 → 8**.

**No usage window exists for this repo**: `usage-report.py` finds no transcripts under `-Users-…-pocket-it` — work on this repo is launched from the session of whatever project is open, so its agents are counted there. The 14-day window below is the launching project's, and is the first one this repo's retros have taken; it is the baseline, not a trend.

- Sessions 11, four of them with `ctx_avg > 250k` (544 / 623 / 367 $ — the three most expensive). Subagents: developer 178, reviewer 134, retro 19, qa 2, planner 7, architect 6. **`general-purpose` launches: 0.**
- 102 agents past 150 turns or 250k context; the ten largest are all `developer`, top one 705 calls / 486k.
- Failure signatures: worktree isolation block **186**, classifier denied 62, guard blocked 57, API error killed agent 27, usage limit 26, sleep blocked 14, `AskUserQuestion` in subagent 4. First count taken; no earlier number to compare.
- `BUDGET|STALL`: 4 BUDGET lines in this repo's whole memory, **0 STALL ever**. The newest (PI-16, 238 vs 157 min) subtracts ~98 min of suspended machine in its own text → ~140 worked, inside budget; unusable as an estimate correction, and already answered by `tasks/PI-58`.

## Patterns and the rule each one became

| Finding | Occurrences | Rule landed |
|---|---|---|
| A case green because its scenario **never happened** — the interrupted run and the completed run leave the same trace; the control's own marker written by a wrapper ending in cleanup | 2 in one PR (#108 AC4 and its control, the second still open as `tasks/PI-64`) | `implementing-common.md` **§4**, folded into the absence-test rule: assert a **witness** produced by the path under test, run the case once without the trigger, never a timing bound. Carries the job-control mechanism (`set -m`, signal the process group) that made the trigger vanish |
| A completeness claim proved with a **filtered** query — `false-coverage-evidence` and `example-not-class` are one class seen from two sides | 2, same day, two tasks | `implementing-common.md` **§7**, sharpening the bullet already there: a filter inside the command is an exclusion too; run the query over the class the claim names with every filter removed, paste the raw count, justify each false positive one line each |
| Same, reviewer side: nothing told the reviewer to re-run a class claim unfiltered | — | `reviewer.md` Step 4, one sentence on the Acceptance-criteria line, pointing at §7 rather than restating it |
| A reviewer hit its ceiling with 2 PRs and delivered **no label, no verdict, no report** | 1, high cost (the orchestrator was left blind; the same work closed in a few turns once the order was dictated by hand) | `reviewer.md`, a block above Step 0: label + verdict first, then findings, then the report; never open the next PR while the previous has no label; a finding too expensive to establish becomes `PLAUSIBLE — {what} — {what would settle it}`, which proposes no correction, never blocks a merge and never becomes the verdict |
| Committed work invisible: a branch with a WIP commit, a registered worktree and no PR, that no script names | 1, unbounded cost | `tasks/PI-65` — mechanical, so no prose: doctor warns, it does not block |
| Nothing relates a task's `**Files**` to what its branch really changed, nor two branches to each other | 1 + a live instance on this board (PI-64's Notes and PI-48's `**Files**` disagree about which file PI-48 touches) | same `tasks/PI-65`, AC3 and AC4 — changed sets from `git merge-base` diffs, never from the declarations the check exists to doubt |

**Where each rule's compliance is checked**: §4 and §7 are readable off the diff by any reviewer (a witness assertion is present or absent; a cited count is filtered or not). The reviewer ordering is observable as the label on the PR — the thing whose absence produced the signal. PI-65 is the guard for the last two; no prose was written for them.

## Lessons: 0 new, 0 promoted, 2 removed (10 → 8)

- `WHERE bin/cleanup-merged.sh · WHEN deciding a branch is merged · DO ask its PR state, never infer from ancestry` — **removed**: shipped (PI-38, Done) and stated in the script's own header and in a project fact; its absolute form is also contradicted by the shipped design, which keeps ancestry deliberately for the one path where it is sound.
- `WHERE bin/doctor.sh · WHEN a task file is written · DO always give it a **Status** line` — **removed**: shipped, `bin/doctor.sh:213` warns by that exact name.
- Nothing new was written here: every rule of this pass landed in a file agents read at start or in a task, which is what makes a lesson a duplicate rather than an addition.
- Deliberate deviation, stated: the 2026-09-16 lesson (`a class visible only in what the program prints`) was **not** promoted, although this pass looked like its second occurrence. Its home is the planner's criterion wording; today's defect was in the test, not in the criterion — AC4 said the right thing. Promoting it here would have put one concept in two homes.

## Facts

- Retracted 1: the `set -m` / SIGINT-pre-ignored fact, now carried in full (SIGTERM caveat included) by the §4 rule in a file every implementing agent reads at start — which is the routing this signal was about. 75 visible, under the 85 threshold, so no hygiene pass.

## Not actioned

- The 186 worktree-isolation blocks, the 102 runaway agents and the four sessions over 250k context: measured, not diagnosed. No breakdown exists of what each block was trying to write, and a rule written from the count alone would be a guess — the numbers are recorded here as the first window so the next retro has a trend to compare.

Nothing in this pass waits on the user: no business decision, no credential, no permission was involved.
