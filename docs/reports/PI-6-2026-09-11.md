# PI-6 — Align the retro thresholds and the agent read budget to the 100-fact cap

## What changed and why
PI-5 raised the handoff fact cap (`bin/handoff.sh` `CAP=100`) but left three prose lines elsewhere still assuming the old cap of 30, which would have undone PI-5 on the next retro run and starved implementing agents of facts beyond line 30. This task updates the three lines to agree with the new cap of 100:

- `.claude/agents/retro.md:47` — "Keep the section ≤ 30 lines" → "Keep the section ≤ 100 lines" (unchanged: still tells the retro to replace a fact now covered by a rule).
- `.claude/agents/retro.md:57` — "Facts hygiene" paragraph: cap `(30)` → `(100)`, trigger `≥ 25 facts` → `≥ 85 facts`, target `≤ 20 facts` → `≤ 70 facts`. Kept the old proportions (83% trigger, 67% target) per the task notes. Everything else in the paragraph (what moves to best-practices, what gets deleted, veto listing) is unchanged.
- `.claude/agents/shared/implementing-common.md:39` — "at most 30 lines" → "the whole facts section, up to the cap of 100 one-line facts". The `awk` command on that line is untouched.

No code was touched: `bin/handoff.sh` already had `CAP=100` as the single source of truth (from PI-5); this task only makes prose agree with it.

## Acceptance criteria → tests
This is a prose-only task (per "Tests expected": no new tests). Verification done directly:
- AC1 — confirmed by reading `.claude/agents/retro.md:47` post-edit: says "≤ 100 lines", still says "replace a fact now covered by a rule".
- AC2 — confirmed by reading `.claude/agents/retro.md:57` post-edit: cap `(100)`, trigger `≥ 85 facts`, target `≤ 70 facts`; rest of paragraph unchanged.
- AC3 — confirmed by reading `.claude/agents/shared/implementing-common.md:39` post-edit: read budget now "the whole facts section, up to the cap of 100 one-line facts"; `awk` command unchanged.
- AC4 — `grep -rn "30" .claude/agents/retro.md .claude/agents/shared/implementing-common.md` run post-edit: remaining hits are "older than 30 days" (fact-promotion age, untouched per instruction), port 3100, `~30 turns` stall rule, and `maxTurns (300)` — none refer to the facts cap.
- AC5 — ran the full `testCommand` (`cleanup-merged.test.sh`, `worktree.test.sh`, `doctor.test.sh`, `handoff.test.sh`, `guard.test.sh`, `guard.heredoc.test.sh`): all green, no regressions.

## Deviations
None. Only the two files in `**Files**` were touched.

## Env vars / migrations / manual steps
None.

## Checks run
```
bash bin/cleanup-merged.test.sh && bash bin/worktree.test.sh && bash bin/doctor.test.sh \
  && bash bin/handoff.test.sh && bash .claude/hooks/guard.test.sh && bash .claude/hooks/guard.heredoc.test.sh
```
All suites reported `ok` for every assertion; `handoff.test.sh: all ok`.

## Turns / stop condition
Well under budget (60 turns estimated for XS). No BUDGET or STALL logged — straightforward prose alignment, no stalls encountered.
