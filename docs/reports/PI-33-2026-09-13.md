# PI-33 — Six deferred rules in implementing-common.md

## What changed and why
`shared/implementing-common.md` was edited in place (never copied into agent files, per its own §0 line):

- §4 "Done well" paragraph: added the rule that a test asserting an absence needs a positive case that fails when the input goes unread (rule 5).
- §4 "Which tests to run" code block: `git diff --name-only $(git merge-base $BASE HEAD) HEAD` → `$(git merge-base "origin/$BASE" HEAD)`, aligned with §6 which already branches from `origin/$BASE` (rule 3).
- New `## 9. Rules from real incidents` section, four bullets, each one imperative line + one-sentence reason:
  - verification of absence described by effect, never by repeating the forbidden content (rule 1)
  - migration/backfill never applied to a real database before review and merge; whoever merges applies it afterward (rule 2)
  - no write to a shared database ever, rolled back or not, even in review (rule 4)
  - resuming an `ALREADY EXISTS` branch: scope is every open PR-comment finding from every round, whatever the resume prompt says; report each closed one by one (rule 6, added per the launch prompt beyond the task file's original five)

Rule 6 was not in the task file's original five; the launch prompt asked for it as a sixth rule plus its own AC, both added to the task file (`## Notes` numbered list item 6, `AC6`).

## Acceptance criteria → checks
- AC1 (rule 1, one-line reason) — §9 bullet 1.
- AC2 (rule 2, who applies the migration and when) — §9 bullet 2: "whoever merges it applies it to the real database, not the task's author beforehand."
- AC3 (§4 uses `origin/$BASE`, consistent with §6) — code block edited, diffed against §6's existing `origin/$BASE` checkout.
- AC4 (rules 4 and 5, each with a one-line reason) — §9 bullet 3 (rule 4), §4 "Done well" paragraph added sentence (rule 5).
- AC5 (no rule contradicts another section) — reread the whole file after editing; no other section discusses absence-verification wording, migrations, shared databases or resume scope, so nothing to contradict.
- AC6 (rule 6, resume scope, one-line reason) — §9 bullet 4.

## Tests
Task declares "Nessun test automatico: è prosa. Rileggi il file intero." No unit test surface: `grep -rl "implementing-common"` across `bin/` and `.claude/` shows only agent docs pointing to the file (`developer.md`, `qa-engineer.md`, `reviewer.md`, `retro.md`, `lessons.md`, the two skill files), none of them a test script that parses its structure. The project's `testCommand` (bin/*.test.sh, guard.test.sh) does not touch this file; not run, as it is unrelated to a prose-only change.

## Deviations
- Placed rules 1, 2, 4, 6 in a new `## 9.` section rather than scattered inline, to keep them easy to verify against the ACs and to avoid rule 2/4 colliding in wording with rule 3's inline §4 edit. Rule 3 (structural) and rule 5 (extends an existing paragraph) were edited in place instead, since they are fixes/extensions to existing prose rather than new standalone rules.
- Added task-file AC6 and numbered-list item 6, as instructed by the launch prompt (not present in the original task file).

## Turns and budget
Well under the 120-turn budget; no `BUDGET`/`STALL` logged.

## Round 2 — NEEDS WORK, 6 findings (PR #57 comment)
Coordinated with PI-28 (#58), which cites these two exact sections by name:
- **"Database terms — real, shared, disposable"** (`### ` subsection under §9) — defines `real` (engine, not mock), `shared` (project config / other sessions / real users may depend on it) and `disposable` (real, created and dropped in this run); every `real DB`/`live DB` mention in §4 now points here and is reconciled with `qa-engineer.md`'s rollback/truncate integration tests (both mean real-and-disposable).
- **"Resume report format"** (`### ` subsection under §9) — `closed — {commit}` or `open — {why}`, one line per finding, none omitted.

Findings closed, one by one:
1. **open — §9 AC5 contradiction** → closed: added the "Database terms" subsection above; §4's `Integration (real DB / real API)` row and the "Integration (live DB)" mention both now read `a disposable DB, §9`.
2. **open — rule 1 not executable** → closed: rule 1 now names every place the string must never land (report, commit, PR, handoff, fixture) and gives the check-without-writing method (`git grep -c -f <pattern file>`, report the count only).
3. **open — rule 2, merger has no way to know** → closed: rule 2 now requires the exact command and target database under the PR's Manual setup steps, plus a line in the 8-line return.
4. **open — rule 5 ambiguous** → closed: rule 5 now specifies the sibling positive-case test and proving it red with the reading step broken, before trusting the absence test.
5. **open — rule 6: "open" undefined, resume-prompt scope only shrinks, report format diverges from #58** → closed: rule 6 now reads every NEEDS WORK PR comment (`gh pr view $PR --comments`), lets the resume prompt only add scope, checks against the branch head (not a commit message), and reports via the new "Resume report format" subsection.
6. **open — task file `**PR**:` empty** → closed: set to `https://github.com/FCabiddu/pocket-it/pull/57`.

Verification: reread the whole file after editing (AC5 recheck) — no other section still uses an unqualified "real"/"shared"/"live" database term outside §9's definitions; `bin/doctor.test.sh` green (unaffected by a prose-only change, confirms nothing else broke). Commit `929f3dd`.

## Round 3 — NEEDS WORK, 2 findings (review `5c45f19`)
Cause: round 2's shared/disposable definitions overlapped (a test container/config file created this run satisfied both), and dropped the "stop and report" clause from round 1's original finding text — fixing the example each time, not the classifying criterion. Resume report format (`### Resume report format`), one line per finding:

- closed — `22f3042`: shared/disposable redefined on two criteria each (round 3 wording; round 4 found them non-complementary — see below). The config file (`.env*`, compose) never decides, only who created the instance and when. Added an edge-case check paragraph (not part of the definition) for the three cases the reviewer named.
- closed — `22f3042`: restored `blocked — no disposable database: {why}` (§8) when a disposable database cannot be created — no mock/other-engine substitute, no silent skip.

Section titles unchanged. Verification: `bin/doctor.test.sh` green.

## Round 4 — NEEDS WORK, 3 findings (review `bba5548`)
Cause: round 3 defined shared and disposable each with its own criteria — a database created in this run and never removed fell into neither; `createdb` on an already-running server read as both (server existed vs database is new); the staging example ("restored from a dump … is disposable") contradicted "existed before this run" and read as authorizing a write to it. Fix, by construction: define **disposable** strictly (one unit — the database you connect to, not the server — created and removed inside this run) and **shared** as its exact complement, every other database, no criteria of its own. Reviewer's text used verbatim (commit `c71b7ca`).

Case → category, run against the new text only (rows 141/143):

| Case | Category |
|---|---|
| Created and not removed (test DB left running for next session) | shared (not removed before run ends) |
| `createdb` on an already-running server | disposable if `dropdb`'d before run ends (unit = the database, not the server) |
| Container started this run and removed before run ends | disposable |
| Persistent local development database | shared |
| Existing staging instance | shared (restoring a dump over it is a write to a shared database — rule 3) |
| Test database the project's config points at | disposable only if created and removed this run; shared otherwise — the config never decides |

Resume report format:
- closed — `c71b7ca`: disposable/shared rewritten with disposable strict and shared as its complement; no case falls into neither or both.
- closed — `c71b7ca`: unit named explicitly (the database you connect to, not the server hosting it) — resolves the `createdb`-on-a-running-server overlap.
- closed — `c71b7ca`: staging example rewritten — an existing staging instance is shared, restoring a dump over it is flagged as a write to a shared database, not as disposal.

Verification: `bin/doctor.test.sh` green; table above walked case by case against the committed text, one category per row.
