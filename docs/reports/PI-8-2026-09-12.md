# PI-8 — report

## What changed and why
`bin/handoff.sh log` truncated the `## Log` section to the last 40 lines with
`lines=lines[:40]`, silently discarding the overflow — a project's narrative
memory that erases without a trace. Rotation now moves the pushed-out lines
to a new `docs/SESSION_HANDOFF_ARCHIVE.md`, append-only (never overwritten,
never re-created if it already exists), and the command reports how many
lines it archived on stdout. The 40-line cap on the live log is unchanged;
only the destination of the overflow changed, matching the refuse-don't-drop
rule already used for the facts cap (PI-2/PI-5).

## Tests ↔ acceptance criteria
All five in `bin/handoff.test.sh`, new `PI-8 …` cases in a fresh sandbox
(`S4`/`S5`) so they do not interact with the pre-existing facts-cap sandbox:
- AC1 → "PI-8 AC1 — total lines (log + archive) grow by one" +
  "PI-8 AC1 — the oldest line (line 1) is in the archive, not in the log"
- AC2 → "PI-8 AC2 — main log still has 40 lines" +
  "PI-8 AC2 — the newest line is on top"
- AC3 → "PI-8 AC3 — line 1 (archived first) survives a second rotation" +
  "PI-8 AC3 — line 2 (archived second) is also there" +
  "PI-8 AC3 — archive has exactly 2 lines after two rotations"
- AC4 → "PI-8 AC4 — no archive file is created below the cap"
- AC5 → "PI-8 AC5 — output names the archived count"

Mutation check per the task's instruction: reverted the fix to the old
`lines=lines[:40]` (no archiving) and re-ran the suite — AC1, AC3 and AC5
cases turned red (5 FAILs), AC2/AC4 stayed green as expected since they only
assert on the untouched main log. Confirms the tests actually exercise the
archiving path. Restored the fix; suite green again.

## Deviations
None from the task's notes. Archive file chosen as
`docs/SESSION_HANDOFF_ARCHIVE.md`, same family as `docs/SESSION_HANDOFF.md`
per the task's own suggestion — no pre-existing archive convention found in
the repo (`find . -type d -name best-practices` and a grep for "archive"
under `bin/`/`docs/` both came up empty).

## Checks run
- `bash bin/handoff.test.sh` — 26 cases, all ok (17 pre-existing + 9 new PI-8 ones — note: some ACs have 2-3 assertions each).
- Full scoped `testCommand` from `.pocket-it.json`: `cleanup-merged.test.sh`,
  `worktree.test.sh`, `doctor.test.sh`, `handoff.test.sh`, `next-wave.test.sh`,
  `guard.test.sh`, `guard.heredoc.test.sh` — all green.
- Manual smoke test in a scratch sandbox: 40 lines, add 41st → 1 line
  archived, log stays 40, newest on top; add 42nd → archive appends to 2
  lines; below-cap run creates no archive file. Matches automated results.

## Turns / stop condition
Well within budget (120); no BUDGET or STALL logged. No blockers.
