# PI-27 — report

## What changed and why
`.claude/skills/deps/SKILL.md` §2 ("Task file") and the old §4 ("Reduce the noise at the source") described the `dependabot.yml` rewrite in two wrong ways: as a line the user approves, and as an edit made from the orchestrating session. Both violate the pipeline's own rules (no business decision for an internal cadence choice; no orchestrator edits to files — every change goes through a `developer` with review).

Fix: folded the `dependabot.yml` condition and rewrite directly into §2, the same task file the skill already writes for the monthly rollup. Removed the old §4 entirely — there is no longer a separate "propose to the user" step. The rollup task's Files list now includes `.github/dependabot.yml` when it qualifies (weekly schedule, no groups), and a new AC4 verifies the rewrite (monthly interval, a `groups` block, minor/patch grouped, majors ungrouped). The change is implemented by the `developer` and goes through the same `reviewer` pass as the rest of the rollup — no new actor, no user approval gate, no orchestrator edit.

## Acceptance criteria → tests
This is a prose-only skill file; no automated tests apply (task says so explicitly). Verified by rereading the whole file after the edit.
- AC1 (dependabot.yml change attributed to the rollup task, executed by a developer and reviewed): met — §2 now states the condition, adds the file to Files, and folds it into the same developer/reviewer flow as §3.
- AC2 (reread in full: not presented to the user for approval, not executed by the orchestrator): met — old §4 removed; no remaining reference to proposing it to the user or editing it from the skill's own session.
- AC3 (rollup task template includes dependabot.yml in Files and a criterion verifying the change): met — §2's Files/AC list now names `.github/dependabot.yml` and AC4 checks its content when it qualifies.

## Deviations
None — single-file prose edit, no other files touched.

## Env vars / migrations / manual steps
None.

## Checks
Full reread of `.claude/skills/deps/SKILL.md` (18 lines) — no remaining mention of user approval or orchestrator edit for `dependabot.yml`; the condition, Files entry and AC4 are present in §2.

## Turns
Well under budget (60). No BUDGET/STALL logged.

## Second round (review)
Reviewer NEEDS WORK on PR #51: AC3's allowed-file enumeration (`package.json`, lockfile, codemod-touched files) did not include `.github/dependabot.yml`, contradicting the clause in §2 that directs the developer to edit that exact file when it qualifies, and AC4 which verifies that edit. Fixed by adding `.github/dependabot.yml` to AC3's list, conditioned on the same qualifying clause used by AC4 ("only when this task's condition triggered its edit"). Reread the whole §2 model again: no other criterion contradicts the allowed-file list or another criterion.
