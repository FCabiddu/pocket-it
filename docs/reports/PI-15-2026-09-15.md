# PI-15 — I lettori passano al compositore

## What changed and why
Ogni punto che leggeva `docs/SESSION_HANDOFF.md` direttamente (awk/grep sul file, o una menzione prescrittiva
del percorso) ora passa da `bin/handoff.sh facts|recent|grep` (il compositore in sola lettura di PI-14).
Nessuna chiamata di scrittura (`log`/`fact`) è cambiata: scrivono ancora nel vecchio file, come previsto
dall'ordine "prima i lettori, poi gli scrittori" del TAD §9.1.

File toccati (tutti gli 8 del campo **Files**, con testo riformulato per non citare più il percorso grezzo):
- `.claude/agents/shared/implementing-common.md` (§3, "First, the facts…") → `handoff.sh facts`
- `.claude/agents/developer.md` (Step 1) → `handoff.sh facts`
- `.claude/agents/reviewer.md` (Step 0) → `handoff.sh facts`
- `.claude/agents/retro.md` (Step 1, blocco bash) → `handoff.sh grep "BUDGET|STALL"` e `handoff.sh facts`
- `.claude/agents/implementation-planner.md` (Budget) → `handoff.sh facts` / `handoff.sh grep "BUDGET|STALL"`
- `.claude/agents/shared/lessons.md` (intro) → "read via `handoff.sh facts`"
- `.claude/skills/run-wave/SKILL.md` (Learning hooks) → `handoff.sh grep "BUDGET|STALL" | head`
- `bin/status.sh:44-47` (ora 44-57) → la sezione finale interroga `handoff.sh facts`/`recent 4` invece di due `awk` diretti, dietro una `handoff_has_memory()` che controlla la presenza dei file congelati o di un frammento sotto `docs/handoff/**` (compatibilità in avanti con PI-16, senza scriverci nulla — il compositore stesso non tocca mai il disco).

**Fuori dal campo Files, ma necessario per l'AC1**: `bin/retro-due.test.sh:286` creava una fixture con
`cat > "$S21/docs/SESSION_HANDOFF.md" <<'EOF'`, che la regex dell'AC1 intercetta come falso positivo (un
`cat` seguito da `SESSION_HANDOFF` sulla stessa riga) pur essendo un test legittimo del percorso di fallback
di `retro-due.sh` (che già usa il compositore quando disponibile, e la lettura diretta altrimenti — non
tocca la memoria di un progetto vero). Corretto assegnando il percorso a una variabile prima dell'heredoc,
senza cambiare il fixture né il comportamento testato.

## Acceptance criteria → tests
- **AC1** — `git grep` con l'espressione dell'AC, esclusi `docs/`, `tech-analysis/`, `tasks/`, `bin/handoff.sh`,
  `bin/handoff.test.sh`: **prima della modifica 12 righe** (misurato, non gli 11 stimati dal TAD — la
  dodicesima è la fixture di `retro-due.test.sh` sopra, comparsa dopo la stesura del TAD), **dopo la modifica
  0 righe** (misurato, `exit=1` da `git grep` = nessun match). Comando e conteggio rieseguiti a mano, non
  serve un test dedicato: è la mutazione stessa.
- **AC2** — `bin/status.test.sh`: due nuovi test creano un repo scratch con un `docs/SESSION_HANDOFF.md` di
  prova, calcolano `expected` con l'`awk` grezzo di prima del task, e confrontano byte per byte l'output di
  `bash bin/status.sh` per `handoff facts:` e `handoff log (last 4):`. Verificato anche sulla memoria reale di
  questo repo: `diff` fra l'output di `main:bin/status.sh` e quello dopo il cambio è vuoto.
- **AC3** — `bin/status.test.sh`: un repo scratch senza `docs/SESSION_HANDOFF.md` né `docs/handoff/**` produce
  esattamente `handoff: nessuna memoria`, esce con 0, non crea `docs/` e lascia `git status --porcelain`
  invariato — tre test distinti.
- **Test aggiuntivo (compatibilità in avanti, non un AC esplicito ma implicito nel Goal "nessuna installazione
  intermedia lascia un lettore cieco")**: un repo scratch con **solo** un frammento sotto
  `docs/handoff/2026-09/*.md` (nessun vecchio file) viene comunque riconosciuto come "memoria presente" e il
  suo fatto compare nell'output. Mutazione eseguita a mano: togliendo il ramo `docs/handoff` da
  `handoff_has_memory()`, i due test relativi vanno rossi (verificato, poi ripristinato).

## Checks
- `bash bin/status.test.sh` — 16/16 ok (9 preesistenti + 7 nuovi).
- `bash bin/retro-due.test.sh` — all ok (comprese le fixture toccate).
- Intero `testCommand` di `.pocket-it.json` (12 suite) — 786 ok, 0 FAIL, exit 0.

## Deviazioni
- `bin/retro-due.test.sh` toccato fuori dal campo **Files** del task, per il motivo sopra (necessario a
  soddisfare l'AC1 alla lettera; comportamento del test invariato).
- Conteggio "prima" dell'AC1: 12 righe misurate contro le 11 del TAD (§12 PI-15) — la fixture di
  `retro-due.test.sh` non era presente (o non contava) quando il TAD è stato scritto. Non cambia l'AC (deve
  restare vuoto dopo), solo la cifra "prima" documentata qui.

## Env vars / manual steps
Nessuno.

Effort: 41 min elapsed (budget 120 min) — agent-a200fa577c0bfe3d3
