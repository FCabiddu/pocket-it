# Retro 2026-09-17 — signals-2026-09-17

**Trigger**: one hand-carried signal, verbatim — `PI-44 — false-coverage-evidence — 2026-09-17 PI-44 PR #84 approved (delta 1): the round-2 report's completeness evidence … contained one false entry.` Narrow targeted pass: a retro ran on this same window hours earlier (`retro-2026-09-16.md`, signal `example-not-class`, PRs #86/#87 merged). `bin/retro-due.sh` reports `nothing`. Its findings are not re-derived, its numbers not re-counted, its text not rewritten.

## The distinctness question, answered first

**Distinct — not a duplicate of `example-not-class`.** That pattern is about a *specification* given as examples. Here the developer did the right thing in kind: after losing round 1 to exactly that, it classified **all 43** `continue`/`return` sites in `bin/cleanup-merged.sh` rather than revisiting the named ones, and built a git-state invariant (`no_branch_silently_kept`) the reviewer proved non-vacuous at two sites it was never written for. The defect moved from the fix to the **evidence for the fix**. Verified independently, not taken from the signal: `bin/cleanup-merged.sh:272/:273/:278` are bare `continue`s — the report files them under "already routes through `keep()`"; they route through nothing.

The pipeline's answer to the signal's own question — *does anything ask how a classification was established?* — is **no**. `implementing-common.md:170` asks for the table of cases; `:171` governs a test's oracle; `reviewer.md:77` ("tests count only if you saw them run") governs *test results* claimed in a PR body. Nothing covers a **coverage argument**: N rows, each a claim, every one established by reading, presented as a completeness proof. The exhaustive shape is what makes it hard to doubt — a list invites "what about the others", a table of 43 does not.

Where the false row landed is the sharp part: at the row where the invariant's own noun changed kind. "Every path that leaves a branch in place" was read as *local* branch/worktree for 40 rows and, in the closing sentence, as *any* branch — and the three misfiled rows are the ones about refs on origin.

## Evidence
- PR #84: merged 2026-09-16, labels `approved`+`Auto-merge`, 3 comments = round 1 (NEEDS WORK, `example-not-class`) + delta 1 (APPROVED). **The signal came from an approval, not a rework** — a non-blocking finding inside a passing review.
- Delta-1 comment read in full: reviewer audited **3 of 43** rows independently (a `gh` stub over a real bare origin) and **1 was false** — protected and no-PR remote branches left on origin, never announced, never counted, summary `0 kept`. Behaviour correct (candidate selection, the reading AC3 rests on), claim false.
- Causes since the 2026-09-16 mark: **1** — PI-46 #88 `first-round`. No repeat cause. BUDGET/STALL: **0 new** (latest are 2026-09-13/14). Isolation blocks 0, `general-purpose` launches 0. Facts 58 → 59, cap 100, hygiene threshold (85) not reached — nothing moved or deleted. `usage-report.py`: no transcripts under this project's resolved path (this work runs from another session's cwd) — same limitation as the last run, the handoff log carries the signal.

## Trend vs the previous retro (2026-09-16)
Cause lines in the window: 5 → 1. Repeat causes: 1 (`example-not-class`) → 0. `base-moved`: 0 → 0 (still closed by PI-41/#83). BUDGET/STALL, isolation blocks, `general-purpose`: 0 → 0, unchanged. The 2026-09-16 rules held: round 2 of #84 was built against the invariant the last retro put in the Notes, and it worked — the residue is one unverified sentence, not a missed path.

## Written this pass
- **Artefact corrected** (move 1 of 3): a dated correction block in `docs/reports/PI-44-2026-09-16.md` at the classification, naming the false row, the measurement, and that the code is right and was deliberately left alone.
- **Project fact (1)**: `reap_remote_branches`' `:272/:273/:278` leave the remote ref on origin unannounced and uncounted **by design**; the `keep()`/kept-count invariant covers local branches and worktrees, never refs on origin — with "do not re-file this as a defect" and the false report sentence named. (The `no_branch_silently_kept` boundary from the same review was already fact 58 — checked, not duplicated.)
- **Method lesson (1 new, provisional)**: mark each row of a coverage classification with how it was established — ran it, or read it — and run the rows where the invariant's nouns change kind; an unrun row is a hypothesis in a table of facts.
- **Confirmed (1)**: the 2026-09-12 "report concludes an effect is absent" lesson → `confirmed`, second independent occurrence on a different project and a different shape, its WHEN widened to "already correct" and its evidence extended with the 43-row case.
- **Removed (1)**: `2026-09-09 · provisional · WHEN launching agents with isolation: worktree · DO run the session from the project repo` — promoted into the orchestrator's CLAUDE.md ("Worktree da qualsiasi cartella"), which also carries the remedy this line lacked (`bin/worktree.sh` + `Worktree:` argument); and it instructs the orchestrator, who does not read this file, while developer/qa/reviewer/planner, who do, cannot act on it. File stays at 40/40.

## PRs
- Facts + lessons + report + correction: **one** PR, merged — this project *is* pocket-it, so the project layer and the method layer share a repo and a working tree; two sequential PRs over the same two files would only manufacture a conflict.

## Proposed changes to pocket-it (the human step)
1. `shared/implementing-common.md` §"Test a class by construction", appending: `When you answer a completeness question with a classification — every call site, every exit point, every occurrence — that table is evidence only if each row says how it was established: "ran" with the command, or "read". Run, at minimum, every row where a noun in the invariant changes kind (a remote ref where the rule says "branch", a directory where it says "file"): that is where a reading silently swaps one definition for another. An exhaustive table asserted entirely by reading is a hypothesis wearing the shape of a proof.`
2. `reviewer.md:77`, after "Tests count only if you saw them run": `The same applies to a coverage argument. When a PR or its report offers a classification of N sites as proof the fix is complete, do not read it — establish three of its rows yourself, chosen where the invariant's terms shift, and say in the approval which three. On PI-44 that sample was 3 of 43 and one was false.`
3. `reviewer.md:21` cause taxonomy, and `bin/retro-due.sh`'s signal types: add `false-coverage-evidence`. Today the taxonomy fires **only on NEEDS WORK**, so a non-blocking finding inside an approval — which is exactly what this was — carries no cause field and raises no signal; this retro ran only because the finding was carried by hand, and `retro-due.sh` still reports `nothing` about it. One occurrence, so proposed rather than written; the script fix is a `/quickfix` on pocket-it, not retro work.

## Not actioned
- PI-46 #88 `first-round` (2026-09-17): a first review, no prior round to explain, no pattern.

## Exception
None. No business, money, credential, client-data or physical-check point in this scope. Every rule, fact and lesson from this pass is written and merged in the same run; only the three template lines above wait for the deliberate human promotion step the pipeline defines.
