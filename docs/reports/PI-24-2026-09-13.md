# PI-24 — round 3 (PR #48 delta review)

## Why this round
Round 2's delta review (f795086..e334819) found the verifications proved the wrong thing:
1. `getAnimations().map(a => a.finished)` hangs forever when any animation on the page is `infinite` (marquee, pulse), so the A.0 screenshot script never finishes on a real page.
2. The "font metrics (preferred)" check in `qa-engineer.md` compared `measureText` **sizes**, not **positions** — wrong on a wrapped multi-line title (whole-string width vs a single-line box) the same way the DOM-rect check round 1 removed was wrong.
3. The pixel-diff tolerance threshold (~200px, "order of magnitude above ~20px noise") passes real clips smaller than that, and the ellipsis/marquee/line-clamp exclusions dropped the whole element instead of just the truncated axis.

## What changed
- `.claude/agents/ux-ui-designer.md` A.0 item 1: run the screenshot script from a scratch folder outside the audited project (`npm i playwright` there once) — the audited folder has no `node_modules`, so `import { chromium } from 'playwright'` and `npx -p playwright` both fail there; report a clear one-line message instead of a stack trace if even that install is unavailable.
- Same file, A.0 item 2: removed the wait on `document.getAnimations().map(a => a.finished)`. Screenshot with `page.screenshot({ animations: 'disabled', caret: 'hide' })` instead — Playwright fast-forwards finite animations and resets infinite ones, so neither a fading-in hero nor a running marquee blocks the shot. `waitUntil: 'networkidle'` replaced with `'load'` (networkidle times out on polling pages, per round-2 review finding 3).
- `.claude/agents/qa-engineer.md` "Rendered layout": rewritten around **pixel-position comparison** as the only proof (where the ink falls against the clipping ancestor's padding box), in four numbered steps: settle without waiting on `getAnimations()`, shoot a fixed padded region, neutralise the *full* clip family (`overflow`, `clip-path`, `clip`, `contain: paint|content|strict`, `content-visibility`, `mask`, `-webkit-mask-*` — not just the two named in round 2), diff only the strip outside the ancestor's padding box with **no tolerance threshold**. `measureText` demoted to an optional per-line filter (baseline position per rendered line, never the whole string, never the primary check).
- Same section, exclusions: rewritten to be per-axis, not per-element — ellipsis/marquee excluded **horizontally only**, line-clamp excluded **below its last line only**, and the exclusion binds to the specific ancestor doing the intentional truncation, not every ancestor in the chain (an undersized wrapper around an ellipsis title still fails on its own vertical clip).
- `tasks/PI-24-nobody-looks-at-the-rendered-app.md` Notes: updated to match — measureText's whole-string form is now called out as wrong the same way the DOM rect is, pixel-position stated as the only valid proof.

## Mutations verified (scratch Playwright harness, not committed to the repo)
Built three throwaway fixtures and ran the round-2 wording against them, then the round-3 fix, in `/private/tmp/.../scratchpad/pw-verify` (outside the repo, discarded):

1. **Animation hang** — a page with a fading hero, a below-fold `IntersectionObserver` reveal, and an infinite marquee. Round-2 script (`getAnimations().finished`, bounded to 15s for the test): no screenshot, still hung. Round-3 script (`animations: 'disabled'`): screenshot in 668ms, hero at full opacity, below-fold heading revealed.
2. **measureText false red** — a title wrapping to 2 lines in a 420px box, no real clip. Whole-string `measureText` width: 530.5px vs a 420px box → false FAIL under the round-2 "preferred" method. Pixel-position diff (as-is vs `overflow: visible`, same region): 0 px → correctly PASS.
3. **Threshold false negative / axis exclusion** — a heading in a box 4px shorter than needed: whole-region diff 68px (round-2's ~200px threshold → PASS, false negative); pixels outside the ancestor's padding box: 15px (round-3, no threshold → correctly FAIL). A separate case: an ellipsis title inside an undersized wrapper clips 1871px outside the padding box on the vertical axis alone — a per-element "ellipsis excluded entirely" rule would have skipped it; the round-3 per-axis rule still catches it because the vertical clip belongs to the wrapper, not to the ellipsis mechanism.

## Acceptance criteria — unaffected (round 1/2 already closed AC1, AC2, AC4, AC5)
AC3 (rendered-layout check on real geometry) re-verified with the corrected measure above.

## Checks
No automated tests — task is agent-definition prose, as stated in "Tests expected" and confirmed unchanged in this round. Full re-read of both edited files after the edits (done above). No lint/build applicable to `.md` agent files.

## Turns / budget
Round 3 alone: ~55 turns (worktree setup, merge-conflict resolution on `docs/SESSION_HANDOFF*.md`, review-findings read, fixture-based verification, edits, commit). Cumulative across all 3 rounds is over the M/200 budget declared on the task; not logging a fresh BUDGET line since round 2 already logged one implicitly via the "needs work" cycle and this round closed on the delta findings, not on a stall.

## Notes for the next reviewer
- The merge with `origin/main` had two real conflicts (`docs/SESSION_HANDOFF.md`, `docs/SESSION_HANDOFF_ARCHIVE.md`); resolved as a union, 0 lines lost — archive taken entirely from `origin/main` (nothing unique on this branch), handoff log now has 45 lines (over the 40 cap; the next `handoff.sh log` call will rotate the overflow into the archive automatically).
- Facts in `docs/SESSION_HANDOFF.md` for "text clipping" and "Playwright settle" were updated in the same merge commit to state the corrected (round-3) mechanism, so the next agent reading facts does not rediscover round 1/2's superseded advice.

## Round 4 — replace the local method with a citation to the (now merged) compass procedure

### Why
The reviewer found round 3's own "diff only the strip outside the clipping ancestor's padding box" method structurally blind to a clip that stays **inside** that box (a `clip-path` polygon or a `mask-image` cutting a letter without pushing any ink past the ancestor's edge) — measured on the reviewer's fixtures: polygon 225px, mask 416px, both **0** outside the box. The round-3 method was the developer's own unproven fix to a reviewer comment, and `qa-engineer.md` and `design-compass.md` prescribed two different measures for the same defect. PI-23, merged separately in the meantime, converged the compass's own **Glyph-clipping verification** section (`shared/design-compass.md` §54-64) on a single method through 5 review rounds: whole-region pixel diff (not a strip), full clipper-class neutralisation **page-wide** (`*, *::before, *::after`), background/border/box-shadow forced transparent and non-text media hidden in **both** shots, zero threshold.

### What changed
- `.claude/agents/qa-engineer.md` "Rendered layout": the four numbered steps (fixed region, ancestor-chain neutralisation, outside-padding-box diff, per-line `measureText` filter) replaced with a citation to `shared/design-compass.md` § **Glyph-clipping verification** by name — read it before running the check, do not reconstruct a variant. Kept, because they are agent-specific and the compass doesn't cover them: settling the page (`animations: 'disabled'`, no wait on `getAnimations()`), the scratch-folder/`node_modules` handling, and the per-axis exclusion for intentional truncation (ellipsis/marquee horizontal-only, line-clamp below-the-last-line-only) — none of which the compass procedure addresses, since it is written for unintended clipping.
- `.claude/agents/ux-ui-designer.md`: no clip-measurement variant existed here to remove — Mode A never proves a clip by pixel diff, it visually inspects full-page screenshots (A.0 captures, A.1 scores from them). Confirmed by grep (below); no edit made, per the instruction to touch nothing beyond what needs it.

### `grep -n -i -E "clip|taglio|measureText|bounding|soglia|threshold"` — every remaining line and why it stands
- `qa-engineer.md:16` — states the rendered-layout check exists and why axe/DOM miss it; no measurement method described.
- `qa-engineer.md:49` — accessibility "what this does not catch" caveat; same, no measurement method.
- `qa-engineer.md:51` (new) — the citation itself: names the compass section, forbids reconstructing a box-geometry/`measureText` variant.
- `qa-engineer.md:55` (new) — the per-axis exclusion for ellipsis/marquee/line-clamp; agent-specific, does not restate or contradict the compass's own diff method, only says which property/axis to leave un-neutralised for an intentionally truncated element before running that method.
- `ux-ui-designer.md:71` — one sentence of rationale for why A.0 (render before scoring) exists; no measurement method.
- `ux-ui-designer.md:167` — motion constraints listing `clip-path` as one of three animatable properties; unrelated to glyph-clipping verification.

### Reviewer's two cases reproduced (scratch Playwright harness, outside the repo, discarded)
Built a fixture with a `clip-path: polygon(...)` cutting diagonally through a headline and a `mask-image` linear-gradient fading out its lower half, both applied directly on the text element (so any clipped ink stays inside that element's own box — no pixel ever crosses the ancestor's padding-box edge):
- **Round-3 method** (diff restricted to outside the clipping element's own padding box): polygon **0** outside-pixels, mask **0** outside-pixels, both declared clean — reproduces the reviewer's finding (their fixture: 225 and 416, both 0 outside).
- **Compass method** (whole fixed-region diff, full page-wide clipper-class + transparency/media reset in both shots, zero threshold): polygon **8261** differing pixels, mask **9574** — both caught. A clean control case (no clip-path/mask) diffs at **0** under the same method, confirming it doesn't false-positive on the same fixture set.

### Turns / budget
Round 4: ~30 turns (pull + merge-conflict resolution on the handoff files again, compass reading, grep-based edit, two-fixture reproduction). Cumulative budget (M/200) remains exceeded across rounds 3-4 combined; not stalling — each round has closed real, distinct findings.
