# PI-16 — round 3: the third file-anchored reader, two assertions that could not go red, the month hazard

Rounds 1-2: `docs/reports/PI-16-2026-09-17.md`, `docs/reports/PI-16-2026-09-18.md`. This round answers the
first review (NEEDS WORK, 4 findings). Every number below is followed by the command that produced it.

**Resume report.** F1 `closed — 65325f1` · F2 `closed — 65325f1` · F3 `closed by fixing — 65325f1` (no task
id needed: the deferral is gone) · F4 `closed — PR body rewritten to the current head`. Nothing left open.

**The base moved twice while this round ran** (`git log --oneline origin/main -4` → `2e4fec1`, `12eaed3`).
Merged before any code (`716f8b8`, one conflict: `tasks/INDEX.md`, resolved by regenerating it) and again at
the end (`196d36b`, clean). Every measurement below is from the final head, re-run after the second merge.

**F1 — the corpus is harvested through the composer.** `bin/retro-due.test.sh`'s QF-1 outcome-word corpus
grepped the two files this task freezes, so it could never see a new word again. Measured on a temp repo
holding this repo's real memory plus one real `bin/handoff.sh log "PI-99 PR #999 parked — …"`: the harvest as
written gave **51 forms, `parked` 0 times**; through `handoff.sh recent --all`, **52 forms, `parked` 1 time**.
It now reads the composer with the frozen pair as fallback, exactly as `install-drift.sh`'s `count_logged`,
and carries the guard that was missing: a word written into a fresh repo by the real script must come back
out of the same harvest. Mutation — the composer branch replaced by `false`, i.e. the pre-fix source:
`FAIL QF1 setup — the harvest reads the memory where writes land`, and **only** that one (120 ok / 1 FAIL),
which is exactly why the freeze was invisible: the non-vacuity sibling stays green off frozen history.

**Census asked for by F1** — `git grep -c "SESSION_HANDOFF" -- bin .claude` summed → 78 lines, all classified.
Composers/readers: `bin/retro-due.sh` and `bin/install-drift.sh` ask `handoff.sh` first and read the pair only
as a fallback for a pre-PI-14 install; `bin/status.sh:47` names the two frozen files (correct — frozen is
forever) and probes fragments through `docs/handoff/**`, which only a change to `handoff.sh` could move and
which `where`'s own AC7 assertions would turn red the same day. Everything else is a **fixture** that builds
a frozen source, the AC8 census grep itself, `bin/verify.test.sh`'s exclusion (already covers both
generations) or `reviewer.md`, which the task's Notes keep out of this change. No fourth live instance.

**F2 — two halves, two assertions, each red on its own.** The old check was one alternation: its first
branch matched 0 lines of the real file (`.` against `**`) and its second occurs in *both* §6 paragraphs.
Each half is now anchored on the literal sentence that states it. The same repair was applied to the
`retro.md` row, whose alternation's second branch subsumed the first. All mutations single-sided, on the
real files, run this round (`scratchpad/mutate.sh`, `scratchpad/mutline.sh` — mutate, run, restore):
M-A delete the only sentence saying what the mechanic does **not** settle → **1 FAIL** (the reviewer measured
that same deletion at 8/8 green before this fix) ·
M-B delete the clause saying what it **does** settle → 1 FAIL · M-C drop the base's method-lessons and
promotion from `retro.md` → 1 FAIL · M-D name the frozen file in `retro.md` again → 1 FAIL · M-G name it in
the `CLAUDE.md` row again → 1 FAIL · M-E delete the base's whole rule paragraph → 5 FAIL · M-F delete this
task's whole mechanic paragraph → 1 FAIL. **On "6 of 7":** that figure came from one composite mutation
(round 2's M17) that touched 6 of the 7 assertions; the 7th was green because the mutation never reached
`retro.md`, not because it cannot fail — M-C and M-D above are its own mutation, red both ways. The block is
now 10 assertions, each with a single-sided mutation that turns it red, plus two non-vacuity siblings.

**F3 — closed by fixing, not deferred.** `MONTH` was read once at start-up and compared against a directory
named at the moment of the write. `monthed()`/`MONTHS_RE()` mirror `dated()`/`undate()` and accept either
month this run can legitimately have written, over all 10 assertions. Proof both ways, with `MONTH` forced
to `2026-08` (a run that started in the previous UTC month): the fixed suite is **all ok, 364**; the same
forcing on the pre-fix file (`git show HEAD:bin/handoff.test.sh`) is **10 FAIL** — the exact 10 sites.

**Also closed, same class, found while opening F2/F3 and not deferred:** the front matter of `retro.md` was
extracted with `sed -n '1,12p'`, a line range — exactly what the base's new planner rule forbids. It is now
the YAML block between the first two `---`, with a sibling asserting the extraction returned it (`60776f2`).

**The fact this round does not write.** Round 2 queued "un guard che prova una scrittura contando le righe di
un file preciso…". The reviewer's fact already on main states the same thing for the whole class of
consumers; one house per concept, so it is dropped. The `## Handoff` block below is round 2's minus that line.

**Checks, final head.** Project `testCommand`, each file run separately, all in the foreground:
13/13 green, **1666 ok / 0 FAIL** (`handoff` 365, `verify` 272, `guard` 222, `cleanup-merged` 206,
`doctor` 197, `install-drift` 133, `retro-due` 121, `worktree` 48, `install-live` 46, `install-live.readme`
24, `status` 16, `next-wave` 15, `guard.heredoc` 1). `bash bin/doctor.sh` → `0 error(s), 0 warning(s),
60 task file(s)`, exit 0. `bash bin/tasks-index.sh` → 60 tasks (PI-59 arrived with the merge).

**For whoever merges.** Unchanged: no env var, no migration, nothing to apply. `bash bin/install-live.sh`
after the merge; `handoff.sh where` exiting 0 is the installed-version signal.

Effort: 101 min elapsed (budget 157 min — the task file's `Budget: 200` is in turns, read at 47 s/turn)
— wt:task-pi-16-writes-immutable-fragments. This round only; rounds 1-2 are in their own reports. Within
budget, no `BUDGET` and no `STALL` logged. Most of it is suite runs: 11 full `bin/handoff.test.sh` passes
(the 7 mutations, the two `MONTH`-forced runs and the green baselines), about 2.5 min each.

## Handoff

Not written from this branch (§6: more than one branch is open). Replay verbatim on the base:

```bash
bash ~/.claude/agents/pocket-it/bin/handoff.sh log "PI-16 PR #93 draft — memory writes become immutable fragments, old files freeze — 365 assertions in handoff.test.sh, suite 13/13"
bash ~/.claude/agents/pocket-it/bin/handoff.sh fact "handoff.sh e handoff_sections.py viaggiano insieme: il secondo è l'unica definizione di dove inizia una sezione e di cosa è un record (PI-43 lo riusa, non lo ricopia)"
bash ~/.claude/agents/pocket-it/bin/handoff.sh fact "una fixture che rende non scrivibile il vecchio target smette di provare qualcosa quando il target cambia: 6 asserzioni verdi su un caso che non esercitava nulla, misurato con un conteggio dei frammenti prima/dopo"
bash ~/.claude/agents/pocket-it/bin/handoff.sh fact "una data letta una volta a inizio suite e confrontata con righe scritte dopo va rossa a mezzanotte locale: misurato alle 00:09 su bin/handoff.test.sh, chiuso con dated()/undate(); la stessa forma vale per il mese UTC, chiusa con monthed() su 10 asserzioni (misurato: 10 FAIL col codice vecchio e MONTH forzato al mese prima, all ok col nuovo)"
bash ~/.claude/agents/pocket-it/bin/handoff.sh fact "un'asserzione 'questo branch non tocca X' confrontata con il ref locale main va rossa quando la base avanza: usare origin/main, misurato con la base 34 commit avanti"
bash ~/.claude/agents/pocket-it/bin/handoff.sh fact "un branch fermo da giorni va rimergiato anche a fine lavoro, non solo all'inizio: su PI-16 la base si e' mossa tre volte durante il lavoro e le misure valide sono quelle rifatte sulla head finale"
bash ~/.claude/agents/pocket-it/bin/handoff.sh fact "un'alternanza in un'asserzione va verificata ramo per ramo: su PI-16 il primo ramo di un grep -qiE matchava 0 righe del file che sorvegliava e il secondo compariva in entrambi i paragrafi, quindi la frase che diceva cosa il meccanismo NON risolve si poteva cancellare con 8 asserzioni su 8 verdi"
bash ~/.claude/agents/pocket-it/bin/handoff.sh log "BUDGET PI-16 round 2 — 238 min contro 157 di budget, di cui ~98 di macchina sospesa; sforato progredendo, nessuno stallo"
```
