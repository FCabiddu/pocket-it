# Retro 2026-09-15 — signals-2026-09-15

**Trigger**: `bin/retro-due.sh` signal, verbatim — `PI-37 — needs-work-cause — 2026-09-15 PI-37 PR #74 needs work — verify.sh RED on retro-due R4F1, pre-existing on main — cause: base-moved`. Scope narrowed by the script to PI-37 only (per the Signals: convention, the broad `gh pr list --search` step was skipped).

## Evidence
- `tasks/PI-37-merged-task-left-not-done.md` → PR #74 (open, `needs-work` label at read time). Reviewer comment: `bash bin/verify.sh 74` RED on `retro-due.test.sh`'s own R4F1 self-check, unrelated to PI-37's three files (`bin/doctor.sh`, `bin/doctor.test.sh`, `run-wave/SKILL.md`). Reviewer verified independently on a standalone `origin/main` checkout: same FAIL, same malformed log line, not introduced by PI-37's diff.
- Root defect already fixed and merged: PI-39 (PR #77, merged 2026-09-15) — `handoff.sh log` now normalizes a redundant leading ISO-date prefix in writing, and corrected the two malformed lines on `main`. R4F1 is green on `main` again; PI-37 is unblocked for a delta re-review (not done here — that is the orchestrator's/reviewer's job).
- `usage-report.py`: no matching transcripts under this project's resolved path for the last 14 days (recent PI-3x work was driven from a different session cwd); not blocking, since `docs/SESSION_HANDOFF.md`'s log carries enough signal for this narrow scope.
- `BUDGET`/`STALL` grep on the log: none in the current window — no estimation or stall pattern in scope.
- Facts: 44 → 45 (well under the 85-line hygiene threshold; no facts moved or deleted this run).
- `shared/lessons.md`: at the 40-lesson cap; no room to append without pruning, so the matching lesson was broadened in place instead.

## Pattern found (1)
**Review rounds — cause `base-moved`, 2nd occurrence** (1st: PI-24/PR #48, 2026-09-13, a shared spec — design-compass — moved under the branch; 2nd: PI-37/PR #74, a shared log line moved under the branch). Both cost a full needs-work round even though the PR's own diff was clean; both were only caught by the reviewer manually diagnosing against a clean base. Rule written: project fact in `docs/SESSION_HANDOFF.md` naming the gotcha and proposing that `verify.sh` itself re-run a newly-red assertion against a clean base tip before reporting RED, labelling a pre-existing base defect distinctly from the PR's own rework. Method lesson (2026-09-11, "mechanical check red on an untouched file") confirmed on this second, independent case; broadened to name both diagnostic techniques.

## PRs
- https://github.com/FCabiddu/pocket-it/pull/78 — merged (fact)
- https://github.com/FCabiddu/pocket-it/pull/79 — merged (lesson confirmed)

## Lessons
- New: 0. Confirmed: 1 (mechanical-check-red-on-untouched-file). Removed: 0.

## Proposed changes to pocket-it
- `implementing-common.md` §verify.sh contract (or `verify.sh` itself): when an assertion is RED, re-run it against a clean worktree of `origin/{base}`'s tip before reporting; if already red there, report `RED — pre-existing on base (cause: base-moved)` distinctly from an own-diff RED. Confirmed lesson above is the evidence; a human should move this into the template/script.

## Not actioned
- PI-39's AC5 deviation (a reader-side tolerance in `NEEDS_WORK_RE` for the doubled-date shape) — already decided against in PI-39's own report as a written proposal, not code; nothing new to add here.

## Exception
None — no business/credential/data/physical-check point in this scope; everything above is written and merged.
