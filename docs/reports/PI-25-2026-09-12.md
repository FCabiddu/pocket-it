# PI-25 — doctor.sh non si accorge di due task con lo stesso id

## Cosa cambia e perché

`bin/doctor.sh` costruiva `tasks[tid] = {...}` dentro il ciclo sui file di board:
quando due file dichiaravano lo stesso id in intestazione, il secondo sovrascriveva
silenziosamente il primo nel dict, senza errore né avviso — la stessa classe di bug
già trovata e corretta in `next-wave.sh` (vedi fatto in `docs/SESSION_HANDOFF.md`,
issue #24), mai portata a `doctor.sh`.

Fix: l'estrazione dell'id per il controllo di duplicazione ora usa la stessa regex
di `next-wave.sh` (`^#\s*([A-Za-z]+(?:-[\w.]+)+)`), così i due script derivano l'id
allo stesso modo — un id **dichiarato** in intestazione, mai una supposizione dal
nome file. Un nuovo dict `id_files` accumula, per ogni id dichiarato, la lista dei
file che lo dichiarano; dopo il ciclo, ogni id con più di un file produce un
`ERROR duplicate task id {id}: {percorso1}, {percorso2}, …`. Il vecchio fallback
per intestazioni non parsabili (`os.path.basename(f).split("-")[0]`) resta invariato
per l'uso esistente di `tasks[tid]` altrove nello script (controlli Depends-on,
DEPS.json), ma non partecipa più al controllo di duplicazione: non è un id
dichiarato, solo una supposizione, e farlo partecipare avrebbe potuto produrre falsi
positivi tra file con intestazioni malformate ma nomi simili.

I file EPIC/STORY di riepilogo restano esclusi come già accadeva: sono filtrati dalla
lista `files` prima ancora del ciclo (stesso filtro preesistente), quindi non entrano
mai in `id_files`.

Allineamento chiesto dalle note del task: la regex di derivazione dell'id ora è
letteralmente la stessa usata da `next-wave.sh` (stesso commento a corredo); non
c'era altra divergenza da riconciliare per questo controllo specifico.

## Criteri di accettazione → test (bin/doctor.test.sh, repo 3 e repo 4)

- AC1 (id duplicato → ERROR con id e i due percorsi) → `AC1 duplicate id is an ERROR with id and both paths`, `AC1 duplicate id fails doctor (exit 1)`
- AC2 (nessun duplicato → nessun output dal controllo) → `AC2 no duplicates: check produces no output`, `AC2 no duplicates: doctor stays green`
- AC3 (id che differiscono solo nel segmento numerico, es. PI-9 vs PI-19, non scambiati) → `AC3 PI-9 and PI-19 not treated as the same id`
- AC4 (file EPIC/STORY di riepilogo non contati come duplicati) → `AC4 epic/story summaries not pulled into the duplicate error`

Prova per mutazione su AC1: rimosso temporaneamente il blocco `for tid, tfiles in
sorted(id_files.items()): ...` da `bin/doctor.sh`, rieseguito `bin/doctor.test.sh` →
i due casi AC1 sono diventati rossi (`FAIL`), tutti gli altri (incluso AC3/AC4)
restano verdi perché non dipendono da quel blocco. File ripristinato dal backup
subito dopo, diff confermato vuoto rispetto al commit.

## Controlli eseguiti

- `bash bin/doctor.test.sh` — verde (14 casi, incluse le 6 righe pre-esistenti PI-3 e
  le 8 nuove per PI-25)
- `shellcheck bin/doctor.sh` — nessun rilievo
- `shellcheck bin/doctor.test.sh` — solo rilievi di stile pre-esistenti nel pattern
  del file (SC2016/SC2001/SC2164/SC2329 sugli helper già presenti prima di questo
  task), nessuno introdotto dalle righe nuove
- Intero `testCommand` di `.pocket-it.json` (cleanup-merged, worktree, doctor,
  handoff, next-wave, guard, guard.heredoc, verify) — tutti verdi
- `bash bin/doctor.sh` lanciato contro il repo principale `pocket-it` (non il
  worktree): **0 error(s), 0 warning(s), 20 task file(s)** — nessun id duplicato
  oggi. Verifica incrociata manuale (`grep` sull'intestazione dei 20 file di
  `tasks/*.md` + `sort | uniq -c`): 20 id, tutti con conteggio 1. Nessun
  `bin/doctor.test.sh` nuovo da aggiungere a `testCommand`: il file esisteva già ed
  è già registrato.

## Turni e budget

Budget 60, stallo/`BUDGET` non necessari — task completato entro budget con lavoro
continuo (commit + test verdi senza blocchi).

## Note per il prossimo agente

Nessuna deviazione. Il worktree conteneva già un commit vuoto (`wip: avvio`) lasciato
di proposito per proteggerlo da uno script di pulizia che oggi cancella branch senza
commit propri (vedi PI-22): non toccato.
