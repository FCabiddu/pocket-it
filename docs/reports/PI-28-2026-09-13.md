# PI-28 — report

## Round 1 — what changed and why
Prose-only change across `run-wave/SKILL.md` and `reviewer.md`: added Step 4b (overlay-test every APPROVED PR of a wave before the first merge), declared its exception to "never the full suite" in `reviewer.md`, added two finding-writing rules, a developer-relaunch scope rule, and replaced the manual-compaction wait with "never suspend work waiting for compaction". AC1–AC6 all marked met after a full reread. See commits 2f4e30b / bc52619 / 2c65e9b.

## Round 2 — NEEDS WORK (8 findings), all closed at afb6287
The reviewer found the overlay step (4b) unexecutable as written and two rules contradicting PI-33 (#57, not yet merged, source of shared definitions). Fixed all 8, coordinated by citation-not-redefinition per the orchestrator's routing:

1. closed — afb6287: executor changed from "main session" to a `reviewer` launched with `Mode: overlay` (new `reviewer.md` Step 3b); `run-wave` 4b only launches it and reads `GREEN`/`RED at PR {branch}`.
2. closed — afb6287: the five command defects fixed — worktree path made **absolute** (`$ORIG/.claude/worktrees/…`; a relative path double-nests once a subshell has already `cd`'d into it once — this is the actual bug the first draft had, found only by running the script, not by rereading it); `git fetch origin` + `origin/$BASE`; conflict now aborts and exits before any test runs; no `cd -` anywhere; tests run after every merge so a 3+ PR wave names the exact breaking PR instead of guessing "entered second".
3. closed — afb6287: `docs/SESSION_HANDOFF.md` / `_ARCHIVE.md` conflicts get the union-merge branch from the reviewer's own comment, run against a real conflicting merge (0 markers left, both sides' lines kept — verified below).
4. closed — afb6287: overlay reruns whenever a PR gets new commits after the tree last went green, not only once after review groups return.
5. closed — afb6287: on red, `run-wave` merges the PRs that tested green before the red one for real first, then relaunches the red PR's developer with cause `base-moved` — its base genuinely was stale, so the failure reproduces on its own branch.
6. closed — afb6287: one suite definition, only in `reviewer.md` Step 3b (whole unit+component, `testCommand` only if it runs everything, never affected, never DB/browser E2E) — removed the duplicate/contradictory phrasing from `run-wave`.
7. closed — afb6287: the "run corrections on real data" rule now cites `implementing-common.md` §9 "Database terms — real, shared, disposable" by name (realistic, disposable-DB data; never a shared-database write) instead of its own definition that permitted running a DB correction during review.
8. closed — afb6287: `quickfix/SKILL.md`'s relaunch prompt gets the same all-rounds-open-findings rule as `run-wave` Step 4, citing "Resume report format" by name. Outside PI-28's `**Files**` — justified: same rule this task's AC6 already covers for `run-wave`, PI-26's "find the mechanism everywhere it lives" applies.

## Verification (round 2)
Built a throwaway repo (two branches with a real conflict on the log-file analogue and a real behavioural dependency) and ran the actual Step 3b script against it, unedited except for filling the three `{…}` placeholders:
- GREEN path: PR1 merges clean and its test passes; PR2's merge conflicts on the log file, union-resolves (0 conflict markers, both lines kept, confirmed by `grep -c` and `cat`), test still passes → `GREEN`, worktree removed.
- RED path (branches reordered so the second PR's merge breaks the test): `RED at PR pr2 — shared.txt missing value=2`, worktree removed, exit 1.
- Caught and fixed a real bug this way: the first draft's relative `$WT` path double-nested once the loop's own outer `cd "$WT"` was combined with each command's own `(cd "$WT" && …)` subshell — every tree operation failed silently into a false "RED" via a cd error, not the real suite result. Not visible from reading the script; only from running it, which is exactly the "correction executed on real data" rule this same PR adds to `reviewer.md`.
- `bash -n` on the extracted, placeholder-filled script: syntax OK.

## Round 3 — NEEDS WORK (4 findings), all closed at abc7f4b
Cause: round 1's union rule was proved on a single conflict shape (both sides modify a file that exists on the base) without saying where it must not be used, and the union branch never checked whether a merge actually succeeded. Round 2's rewrite inherited both gaps.

1. closed — abc7f4b: text now states the union result on `docs/SESSION_HANDOFF*.md` is **not a correct handoff file** (measured by the reviewer: log over its 40-line cap at 45, 2 lines duplicated between log and archive) and is acceptable only because the overlay tree is detached, never pushed, removed at the end. Explicitly excluded from Step 1's conflict resolution, any pushed merge, `.gitattributes` `merge=union`.
2. closed — abc7f4b: the union branch now falls back to an empty base when the common-ancestor stage is missing (add/add) instead of failing `git show :1:` silently; every merge attempt is followed by `git merge-base --is-ancestor "origin/$b" HEAD`, so an unresolved conflict, a failed `git commit --no-edit`, or a merge that produced no diff and no error all become `RED`, never a false `GREEN`.
3. closed — abc7f4b: "never against a shared database and never during review" → "never against a shared database, during review included" (removed the self-contradiction with the rule's own opening clause).
4. closed — abc7f4b: Step 3b's `PRs:` description aligned with `run-wave`'s: only APPROVED branches enter the overlay, parked PRs never do.

## Verification (round 3)
Reproduced the reviewer's own toy-repo shape on fresh fixtures, ran the actual (fixed) Step 3b script unedited except for the three `{…}` placeholders:
- `pa pb pc` — `pa` and `pb` both independently add `docs/SESSION_HANDOFF.md` (real add/add), `pc` changes `code.txt`. Merging `pb` after `pa` produced a real `CONFLICT (add/add)`; the union branch resolved it with 0 markers, both sides' lines kept (`- pa archive` / `- pb archive`); `pc`'s change landed (`code=CHANGED`); result `GREEN`.
- `pa nope pc` — `nope` does not exist on `origin`. Old code (no ancestor check) would have let this pass silently to the next PR; the fixed script produced `RED at PR nope — not merged into the overlay tree`, worktree removed, exit 1 — exactly the class of false-`GREEN` finding 2 named.
- `bash -n` on the extracted, placeholder-filled script: syntax OK.

## Acceptance criteria → status
AC1–AC6 (see Round 1) remain met after rounds 2 and 3's rewrite of the overlay mechanism's internals; the criteria's shape (who runs it, what red/green mean, single-PR skip, cause routed to the right file) is unchanged, reverified by full reread plus the executions in rounds 2 and 3.

## Deviations
- `quickfix/SKILL.md` touched outside `**Files**:` — justified in round 2 (finding 8).
- Did not touch `shared/implementing-common.md` (PI-33's lane, not yet merged); rules cite its section titles by name without restating their content, per the orchestrator's routing message.

## Checks
Prose files, no lint/typecheck applicable. Full reread after every edit. Live execution of the overlay script against real git repos in all three rounds — round 1's own real-data rule applied to itself each time, and it caught two real bugs (the relative-path double-nesting in round 2, the false-`GREEN` on an unmerged/conflicted branch in round 3) that rereading alone had not surfaced.

## Turns / budget
Rounds 1–3 well under budget (120 total). No BUDGET or STALL logged.
