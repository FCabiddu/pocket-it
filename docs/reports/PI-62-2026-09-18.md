# PI-62 — doctor fails when tasks/INDEX.md no longer matches the task files

## What changed and why
`tasks/INDEX.md` was generated by `bin/tasks-index.sh` but nothing verified the step actually ran
after a merge, so a stale row (e.g. a task the file marks `Done` still reading `Todo`, with no PR
link) reached `bin/status.sh` unnoticed — the orchestrator would then open on a board state that
disagreed with the disk it claims to trust.

Two changes, one home for the invariant:
- `bin/tasks-index.sh` gains a `--stdout` mode: it prints the generated table to stdout and never
  touches `tasks/INDEX.md`. The table-building logic itself (the `gen()` function) is untouched —
  only the sink changed (redirect vs stdout) — so there is exactly one implementation of the table,
  never two that could drift apart. Every existing caller (no `--stdout` flag) keeps writing the
  file and printing its `tasks-index: wrote …` line, unchanged.
- `bin/doctor.sh` gains a new check (`# 2d`, right after the PI-37 block, board section) that calls
  `tasks-index.sh --stdout`, compares the result against the committed `tasks/INDEX.md`, and
  **errors** (not warns) when they disagree, naming the task IDs of the differing rows and the exact
  one-liner fix command. Row comparison is cell-based (split on `|`, cells trimmed) so that a
  markdown-reformatting commit hook changing only pipe spacing can never manufacture a false
  positive; if the two texts differ but no row's cells actually differ, the message says so
  ("formatting-only difference") instead of blaming a row — verified manually (see Checks).

The check skips in silence, same house style as the rest of section 2: no `tasks/` directory at all
(AC5), or no committed `tasks/INDEX.md` yet at all (a board that has never run `tasks-index.sh` is a
different gap from staleness — there is nothing to compare against, and every one of this suite's
own pre-existing fixtures, repo1–repo19, has no INDEX.md and must stay green).

## Acceptance criteria → tests
All in `bin/doctor.test.sh`, repo20 series (a–g), inserted after the repo19 mutation-sanity block:
- AC1 (in-sync board → no error/warning, exit unchanged) → "AC1 index in sync: repo2's own
  assertions (0 errors, 1 warning, 2 task files) still hold once indexed" + "AC1 index in sync: no
  index-related error or warning is emitted" (reuses repo2's own fixture, re-asserts its existing
  line still holds once indexed — required by the task's Tests-expected note).
- AC2 (Status/PR changed, index untouched → error naming the ID) → "AC2 Status changed…", "AC2 …
  the unrelated task is not named", "AC2 … counted in N error(s), exit 1", "AC2 … prints the exact
  self-contained fix command", plus a second fixture for the **PR** field ("AC2 PR changed…").
- AC3 (task added / deleted, index untouched → error naming the ID) → "AC3 task file added…" /
  "AC3 task file deleted…", each with a negative check that the unrelated/surviving ID is *not*
  named.
- AC4 (stdout mode: never writes the file; same bytes as write mode; old callers unaffected) →
  "AC4 --stdout mode: tasks/INDEX.md is never created…", "… identical bytes to what the writing
  mode puts in the file", "… an EXISTING tasks/INDEX.md is left byte-identical", "… keeps its mtime
  (never opened for writing)" (both content- and mtime-based, per the task's own note), "AC4 no new
  flag: writing mode is unchanged, still prints its own 'wrote' line".
- AC5 (no `tasks/` at all → silent) → "AC5 no tasks/ directory: no index error or warning".
- Regression (no `tasks/INDEX.md` committed at all, never run → silent, not an error) → its own
  fixture, direct assertion.

## Mutation proof (§4)
Broke the detection branch (`if diff_ids:` → `if False and diff_ids:`) and reran
`DOCTOR_TEST_NO_RECURSE=1 bash bin/doctor.test.sh`: exactly the 4 AC2/AC3 detection tests went red
(`AC2 Status changed…`, `AC2 PR changed…`, `AC3 task file added…`, `AC3 task file deleted…`),
nothing else. Reverted; full suite green again (0 FAIL / 197 ok on the no-recurse pass).

## Changes
- `bin/tasks-index.sh` — `--stdout` mode (arg parsing + `gen()` extraction, sink chosen by mode).
- `bin/doctor.sh` — new `# 2d` board-index-drift check.
- `bin/doctor.test.sh` — repo20 a–g fixtures/assertions (PI-62).
- `tasks/PI-62-doctor-catches-a-stale-board-index.md` — Status/Branch/PR fields.
- `docs/reports/PI-62-2026-09-18.md` — this report.

## Secrets / env vars · Manual setup steps
None.

## Checks
- `bash -n bin/tasks-index.sh`, `bash -n bin/doctor.test.sh` — OK.
- `python3 -m py_compile` on doctor.sh's own heredoc body — OK.
- `bash bin/doctor.sh` on this repo's real board (63 task files, in-sync index): `doctor: 0
  error(s), 0 warning(s), 63 task file(s)` before and after a deliberate one-field mutation
  (mutated → 1 named error; reverted → 0 again).
- `DOCTOR_TEST_NO_RECURSE=1 bash bin/doctor.test.sh`: 197 ok, 0 FAIL.
- `bash bin/doctor.test.sh` (full, with its own PI-45 recursion): 215 ok, 0 FAIL.
- Manual check of the formatting-only branch (extra spaces inserted around one cell's pipes,
  content unchanged): doctor printed `tasks/INDEX.md differs from tasks-index.sh output but no
  task row differs (formatting-only difference) — regenerate it: …`, not a row name.
- Project `testCommand` (13-script chain from `.pocket-it.json`): ran in full. Every script through
  `bin/doctor.test.sh` (my target) is green. `bin/verify.test.sh` reports 6 pre-existing FAILs
  (`PI-34 AC2 SIGINT (test-run phase)`, `PI-41 AC5 SIGINT/SIGHUP during the base re-check`) —
  **inherited, not introduced by this change**: `bin/verify.sh` and `bin/verify.test.sh` are
  outside this task's `**Files**`, untouched by this diff, and the same 6 lines reproduce
  identically on a clean clone of `origin/main` (checked twice) and on this branch run standalone
  (3 of 4 runs; the fourth, run before any load, was clean) — a load-sensitive SIGINT/SIGHUP timing
  test on this shared machine, not a regression from PI-62. Because `testCommand` is `&&`-chained,
  the chain stopped there; `bin/install-drift.test.sh`, `bin/install-live.test.sh` and
  `bin/install-live.readme.test.sh` were run separately afterwards and are green (0 FAIL each).

## Notes / deviations
- The task's AC list does not cover a board with task files but **no committed `tasks/INDEX.md` at
  all** (never indexed, as opposed to stale). Treated as a third skip condition alongside AC5's "no
  `tasks/`" — there is nothing to compare against, and every pre-existing fixture in this suite
  (repo1–repo19) has no INDEX.md, so treating it as an error would have broken 2 of the suite's own
  existing assertions (`doctor: 0 error(s), 1 warning(s), 2 task file(s)` at repo2, and the 5-warning
  line at repo14). Documented as a regression test in repo20g.
- `bin/verify.test.sh`'s 6 FAILs (see Checks) are reported here for visibility but are out of this
  task's scope (`bin/verify.sh` is not in `**Files**`) and pre-exist this change on `origin/main`.

## Handoff
Other task branches are open in parallel right now (`origin/task/pi-32-guard-knows-caller`,
`origin/task/pi-48-cleanup-test-reuses-gate`, `origin/task/pi-6-retro-thresholds`), so per §6 this
write stays off this feature branch. To be replayed on the base branch with the tool:

```
bash ~/.claude/agents/pocket-it/bin/handoff.sh log "PI-62 PR #107 draft — doctor errors on stale board index — 18 new tests"
```

Effort: 34 min elapsed (budget 90 min) — wt:task-PI-62-doctor-catches-a-stale-board-index
