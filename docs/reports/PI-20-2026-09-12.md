# PI-20 — Report

## What changed and why

Three prose files predated PI-19 and still said the opposite of what it established.

1. `tasks/PI-1-worktree-script.md` — the Goal sentence named the orchestrator's
   private entry point. Rewritten to describe the situation (the orchestrator
   can run from a hub folder governing several projects) without the name,
   same sense preserved, matching the phrasing PI-19 already landed for the
   equivalent sentence in `.claude/skills/run-wave/SKILL.md`.
2. `.claude/agents/shared/lessons.md`, the 2026-09-09 "two waves" lesson —
   its `DO` clause read `/compact` as something the orchestrator does. The
   model cannot start a compaction (no tool, hook or command does it, and a
   running session cannot read its own context usage — confirmed, not
   assumed). Rewritten so the `DO` clause is "state the call to compact —
   only the user can press it — or open a new session before the third",
   reusing the distinction already approved in `run-wave/SKILL.md` §Session
   hygiene (the call is the orchestrator's, the keystroke is the user's).
   Only this one line touched; the file's ordering and other lessons are
   untouched, per the task's note that `lessons.md` belongs to the retro.
3. `CLAUDE.md`, the "Cost rules" table row — same defect, same fix: "two
   waves per session, then `/compact`" became "two waves per session, then
   the orchestrator states the call and the user presses `/compact`".

## Acceptance criteria → verification

- AC1 — `tasks/PI-1-worktree-script.md` re-read after the edit: the entry
  point's name is gone from the sentence and the sentence still explains why
  the script exists (session cwd vs. hub-run orchestrator). Met.
- AC2 — `shared/lessons.md` and `CLAUDE.md` re-read after the edit: both now
  describe compaction as something the orchestrator calls for and the user
  executes, in words consistent with each other and with
  `run-wave/SKILL.md`. Met.
- AC3 — Verified by effect, without naming the entry point anywhere in this
  report or in the command used: a case-sensitive recursive search of the
  full working tree (excluding `.git`) for the private name, built so the
  literal string never appears in the command text either, returned zero
  matches. This branch carries no other commits yet, so there is nothing yet
  to check in commit messages beyond the one about to be written — its
  subject and body do not use the name either. Met.

## No automated tests

Prose-only task, as the task file states. No unit/integration/E2E added or
required; `tests.integration`/`tests.e2e` are `off` in `.pocket-it.json` and
irrelevant here regardless.

## Checks run

- `grep -rIn --exclude-dir=.git` full-tree search for the private name
  (built without the literal string in the command): 0 matches.
- Manual re-read of all three edited files.
- No lint/typecheck/build/test applies to a prose-only change; `testCommand`
  in `.pocket-it.json` targets `bin/*.test.sh` and hook tests, none of which
  this change touches.

## Deviations

None. Scope stayed inside the three files listed in `**Files**`.

## Turns / stop condition

Well under budget (60). No `BUDGET` or `STALL` logged — straightforward
prose fixes, no stall, no overrun.
