# PI-23 — report, giro 5

## Cosa non bastava del giro 4

Delta review (`fe160d0..54c54b4`), causa dichiarata: la procedura era stata validata sulla
fixture del developer, non sulle tecniche della compass che la citano. Due finding:

1. `:60-61` (passi 2 e 3) — l'ambito di "on every ancestor" / "the element and its
   ancestors" non è definito: un taglio sull'elemento stesso o su un discendente (proprio
   la #1, che maschera con un `.line` interno, non l'`h1`) sfugge a una lettura letterale.
2. `:120` — "Verify both — hidden-at-rest and pitch-unchanged — per Glyph-clipping
   verification" non è vero: applicata allo stato nascosto corretto, la procedura la
   boccia (confronta pixel contro il clipper neutralizzato, e a riposo il clipper sta
   facendo il suo lavoro — la differenza c'è sempre, indipendentemente da quanto sia
   nascosta bene la riga).

## Correzione (applicata come misurata dal reviewer, non reinventata)

**Passi 2 e 3** — ambito esplicito: `*, *::before, *::after` su tutta la pagina (non
antenati soli, non l'elemento soli — un taglio può stare in una qualunque delle tre
posizioni, elemento/antenato/discendente, e la #1 stessa lo dimostra); media nascosti in
entrambi gli scatti (`img, video, canvas, svg, iframe { visibility: hidden }`), perché si
confrontano solo i glifi.

**`:120`** — tre prove distinte al posto di una:
- **stato finale**: Glyph-clipping verification (invariata).
- **nascosto a riposo**: scatto a riposo contro lo stesso scatto con `* { color:
  transparent }` — 0px atteso.
- **passo invariato**: differenza di `offsetTop` fra due `.line` consecutive = `line-height
  × font-size`, entro l'arrotondamento di 1px del layout — l'unica misura di layout
  ammessa in questo file, perché misura la spaziatura, non l'inchiostro.

## Tabella tecnica → misura con difetto → misura pulita

Script `round5.mjs`, procedura scritta nella compass eseguita alla lettera (Playwright/
Chromium, soglia 0, ambito pagina intera):

| Tecnica / controllo | Caso con difetto | Misura | Caso pulito | Misura |
|---|---|---|---|---|
| Ambito (sanità del fix) | taglio sull'antenato | 88 | — | — |
| Ambito (sanità del fix) | taglio sull'elemento stesso | 2165 | — | — |
| Ambito (sanità del fix) | taglio sul discendente (markup #1) | 1131 | — | — |
| #1, stato finale (maschera permanente) | nessun rimedio di padding, line-height 0.9 | 1231 | `padding:.25em 0;margin-bottom:-.5em` | 0 |
| `:120`, nascosto a riposo | `X = .25em` | 97 | `X = .5em` | 0 |
| `:120`, passo invariato | `padding-bottom:.2em` da solo | 108px (atteso 88.32px, Δ19.7px) | `padding:.25em 0;margin-bottom:-.5em` | 88px (Δ0.3px) |
| #13, polygon morph | poligono che taglia una lettera | 85 | poligono che non tocca il testo, sfondo opaco | 0 |
| #13, polygon morph | — | — | poligono taglia una riga decorativa, testo pulito accanto | 0 |

Le tre righe "ambito" non sono una tecnica della compass ma la prova che la correzione del
finding 1 funziona: con l'ambito pagina intera, un taglio in una qualunque delle tre
posizioni (antenato/elemento/discendente) viene sempre trovato — prima del fix, una lettura
letterale del testo vecchio ("on every ancestor") mancava le altre due (misurato dal
reviewer: 0 su elemento ed 0 su discendente, letture fedeli al testo di allora).

## Criteri di accettazione

AC4 invariato nella sostanza; ora la procedura ha un ambito esplicito e i due controlli di
`:120` hanno una prova propria invece di riusare (a torto) quella del taglio.

## Deviazioni

Nessuna dallo scope file (`.claude/agents/shared/design-compass.md`). Correzione applicata
come scritta nel commento del reviewer (ambito `*, *::before, *::after`, media nascosti),
non reinventata. Script di misura fuori dal repo (scratchpad locale).

## Checks

Nessun lint/build/test si applica a un file markdown di prosa in questo repo. Script di
misura rieseguito fino in fondo, 13 casi, output sopra.

## Turni / budget

Budget S(120) già superato (loggato al giro 4). Progressione: 5 giri di review, ognuno ha
chiuso un difetto reale distinto (formula, offset limite, classe ritagliatori, metodo
doppio+soglia, ambito). Nessun nuovo BUDGET da loggare in questo giro (stesso sforamento
già segnalato), nessuno STALL.
