# PI-41 — verify.sh tells a defect the PR introduced from one it inherited

**Branch**: task/pi-41-verify-base-red-attribution · **Label**: DevOps · **Risk**: high · **PR**: PRNUM

## What changed and why
A failing check is re-run **once**, in a second throwaway worktree at the tip of `origin/<base>`, and the verdict says whose defect it is. The caller reads the **exit code**: `0` green · `1` the branch's own red (text still exactly `verify: RED`) · `2` verify could not run · `3` `verify: RED — pre-existing on base (cause: base-moved)`. `1` is the *worse* of the two reds, so a mixed run exits 1: inheriting a defect never excuses introducing one. Three distinct integers, one meaning each, documented in the script header and in CLAUDE.md's table — nothing to confuse. Per failing check the output adds one line: `own …`, `base …` (plus a `note` line stating the granularity) or `warn … attribution unknown: <reason>`.
Cost: the whole block sits inside `if [[ $NFAIL -gt 0 ]]`. A green run creates no second worktree and re-runs nothing.
Honest limit, stated in the script, in the output and in reviewer.md: attribution is **per check, never per assertion**. No heuristic on failure text was attempted — normalising runner output (timings, counts, paths) would have made the motivating case itself read as "own", i.e. a false accusation.

## Acceptance criteria → tests (`bin/verify.test.sh`, 69 PI-41 assertions, 107 total, ALL PASS)
- AC1 → `AC1 — an own-diff red still exits 1…` · `— still prints exactly 'verify: RED', unqualified` · `— the failing check is named as this branch's own` · `— nothing … mentions a pre-existing defect`.
- AC2 → `AC2 — an inherited red exits 3, a code no other outcome uses` + verdict/line/`note` assertions, and the mutation both ways: `AC2 mutation — base made green: … now exits 1, not 3` / `— base broken again: … flips back to exit 3` (same branch, same command, only the base moved).
- AC3 → `AC3 — the check the branch broke is attributed to the branch` · `— the check it inherited is attributed to the base, in the same run` · `— the verdict is the worse of the two: exit 1, never 3`.
- AC4 → `AC4 — no worktree is created for the base when nothing failed` · `— the passing check ran exactly once` · `— a failing check is re-run against the base exactly once (2 runs in total)` · `— each failing check … no more`.
- AC5 → the fixture's check script records `pwd -P`, so two assertions prove where each run happened (branch worktree, then `<repo>/.claude/worktrees/verify-base-*`, never the main checkout, never /tmp); the guard is the same function, bound to whichever worktree is current. Signals: `kill_in_base` waits until the base worktree is registered, sends a **real** TERM/INT/HUP, then checks exit 143/130/129, no verdict printed, neither worktree left registered or on disk.
- AC6 → four unattributable families, each red-then-plain, none ever printing "pre-existing": worktree refused (a) · base ref unresolvable (b), with no worktree even attempted · the check erroring 127 on the base (c) · the base having no script by that name (d, alongside a second check attributed normally in the same run).

## Mutations executed (applied to the real file, suite run, then reverted)
| mutation | what went red |
|---|---|
| `0) ATTR=own` → `base` (a base that passes is blamed anyway) | 10 assertions: AC1 ×4, AC2-mutation ×3, AC3 ×3 |
| `for w in "$WT" "$WT_BASE"` → `"$WT"` (traps sweep one worktree) | 6: all three signals, registered + on disk |
| `$NFAIL -gt 0` → `-ge 0` (base worktree built on a green run) | `AC4 — no worktree is created … when nothing failed` |
| `126\|127) unknown` → `base` (a check that could not run called pre-existing) | 3 × AC6c |
| `exit 3` → `exit 1` (the two reds share one code) | `AC2 — an inherited red exits 3…` + the mutation-back one |
| reviewer.md back to "RED → NEEDS WORK immediately", exit code piped away | 5 caller assertions |
| CLAUDE.md wording back to "red = needs work" | the stale-duplicate guard |

## Deviations from `**Files**`
`.claude/agents/reviewer.md` + `CLAUDE.md`: the task's Goal is that *the reviewer acts without re-deriving*. Step 3 of reviewer.md piped verify.sh into `tail -40` (exit code discarded) and said "**RED → NEEDS WORK immediately**" — the very sentence that charged two PRs for the base's defect. It now reads the code and routes 1/2/3 differently; CLAUDE.md's two copies of the rule are aligned, and a test fails if any instruction file reduces it to "red = needs work" again. Nothing else outside the two files was touched.

## Checks
`bash bin/verify.test.sh` → ALL PASS. Every other suite in `testCommand` → exit 0, one by one. `bash bin/doctor.sh` → `0 error(s), 0 warning(s), 41 task file(s)`. `bash bin/verify.sh task/pi-41-verify-base-red-attribution main` on this repo → `verify: GREEN`, exit 0. `git worktree list` after every run: no `verify-*`/`verify-base-*` entry.

## For the next agent
A grep filter that excludes paths by **absolute** path is vacuous when the suite runs from an agent worktree — the worktree's own path contains `/.claude/worktrees/`, so the filter drops every file in the repo. Measured here: the stale-duplicate guard stayed green with the defect reinstated. Match repo-relative paths from the repo root instead.

Effort: 35 min elapsed (budget 200 turns, read at 47 s/turn = 157 min) — wt:task-pi-41-verify-base-red-attribution
(start taken from the worktree's own creation time, 20:56:17, not from a `date +%s` in my first command.)
