# PI-21 — report (round 2, after 5 reviewer findings)

## What changed and why (final state)
Round 1 fixed the three defects the task named, plus a mid-task owner correction on AC2 (root cause before escalation). The reviewer's second review found the same principle had not been searched for as a whole class across the repo — round 1 fixed only the cited spots. Round 2 closes that gap.

- **F1 (no escalation, anywhere, as a first move).** `run-wave` §3 (stopped-developer resume) rewritten: cause-first, same model unless the analysis names the model as the cause, relaunch-from-scratch only if the worktree is gone, park only when the cause is out of the pipeline's reach — not on "the cause came back once" (that was a count in disguise).
- **F2 (the analysis needs an input, a destination, an owner per fix).** `reviewer.md` Step 0 now requires a `cause`/`fix at` line on **every** NEEDS WORK (not just delta), carried into **both** the PR comment and the Step 6 report line (the orchestrator never reads the PR comment). `run-wave`/`quickfix` read that report line and name who applies each family's fix: task-file edit on the base branch + pasted into the relaunch prompt (example-not-class); the developer's relaunch prompt as first instruction (base-moved); `shared/implementing-common.md` + `reviewer.md` Step 3 named as the destination, not rewritten elsewhere (verification-reintroduced). New handoff line: `ROUND {ID} PR #{n} round {k} — cause: {…} — fix at: {…}`, read by `retro.md` alongside `BUDGET`/`STALL`.
- **F3 (the cap wasn't derived from the stall's own cause).** Reviewer group cap corrected from 4 to **3 PRs**, using the measured cost already on `main` (`shared/lessons.md`, ~20 turns/PR once merged-tree runs and mutations are counted) instead of the flat 60/7≈8.6 average: 60/~20 ≈ 3. `shared/lessons.md` untouched, per the owner's instruction — it already said "about 3".
- **F4 (what the pipeline solves alone wasn't reported; only-that-point-stops was missing from retro).** `reviewer.md` Step 3: when a batch reviews several PRs against the same base, merge them onto the base in a throwaway worktree and run lint/type-check/full unit suite on the result before approving any — this is what catches a base drift with no textual conflict. `run-wave`/`quickfix` reports gain a `Decided on its own: …` line (causes found, PRs parked, tasks split, `Depends on` fixes, agents resumed) even when nothing needed the user. `reviewer.md`'s "never put to the user" reworded to "never asked of the user, only reported to them". `retro.md`'s human-exception line now says explicitly that only that one point waits, everything else lands the same pass.
- **F5 (contradictions outside the four files).** `CLAUDE.md` (lines 16, 68, 162), `README.md` (28, 57), `.claude/skills/deps/SKILL.md` (20) aligned to the same mechanism — one-reviewer / two-rounds / "propose to the user" language replaced. Scope note: PI-20 (which held `CLAUDE.md`) is merged, so the task's original exclusion of it no longer applies; `Files` and Notes updated to say so.

**Left alone, deliberately:** `.claude/agents/shared/implementing-common.md` (PI-22 is working on it in parallel; the coordinator adds the verification-reintroduced rule there after PI-22 merges) and `shared/lessons.md` (retro's file, already correct at "about 3").

## Acceptance criteria → verification (unchanged from round 1, re-verified after round 2's edits)
- AC1: `run-wave` §4, cap now 3, reasoned from the measured ~20 turns/PR.
- AC2: cause-first everywhere a stop or a red PR could occur (dev blocker in `run-wave` §3, review rounds in `run-wave` §4 / `quickfix` §4), applied with a named owner and destination per family, park only after analysis.
- AC3: `reviewer.md` Step 6 `ORCHESTRATOR:`/`HUMAN:` split (reworded), `retro.md`'s human exception now says only that point waits.
- AC4: reread all seven files (four original + three added in F5) whole; grepped several phrasings (`user (decides|should|...)`, `goes to the user`, `say so and stop`, `one reviewer per wave`, `two fix rounds`, `relaunch a fresh`, `it recurs after`, `as a line the user`) — zero hits after round 2's edits.
- AC5: every remaining human-only stop still names the single point and continues the rest; unaffected by round 2 except for retro's added "only that point waits" sentence.

No automated tests: prose/config files, as the task specifies. `bash bin/doctor.sh`: 0 errors, 0 warnings, 16 task files, after both rounds.

## Deviations
Two mid-task corrections from the task owner, both recorded in the task file's Notes: (1) AC2 became cause-first-then-escalate instead of escalate-first, evidenced by three real multi-round PRs; (2) round 2 widened scope to `CLAUDE.md`, `README.md`, `.claude/skills/deps/SKILL.md` (now in `Files`) and to searching each finding's whole class in the repo rather than only the cited line, per the reviewer's F1–F5.

## Turns / stop
No stall, no budget overrun across both rounds. Prose-only task, within the 120-turn budget.
