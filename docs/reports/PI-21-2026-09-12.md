# PI-21 — report (round 3, final)

## What changed and why (final state)
Three rounds. Round 1 fixed the three defects the task named, plus a mid-task owner correction on AC2 (root cause before escalation). Round 2 found the same principle hadn't been searched as a whole class across the repo, and widened scope to `CLAUDE.md`/`README.md`/`deps/SKILL.md`. Round 3's own cause, per the owner: round 2 had introduced three *new* mechanisms (a persistent `ROUND` log line, a merged-tree test in the reviewer, a direct `dependabot.yml` edit by the orchestrator) without checking each against the rules already next to it — partly the owner's own gap, having dictated those three in a few lines without specifying or verifying them. The fix for that cause is to **reduce scope, not add another layer**:

- **D1 — removed the `ROUND` handoff-log line**, from `run-wave`, `quickfix` and `retro.md`. It had five structural problems (log rotation at 40 lines losing early events, an unanchored grep, a taxonomy not shared identically across four files, an unsafe field separator, an undefined cross-session counter) and the real fix — where budget/stall/round-type events live — is PI-12's design, already in PR; not this task's to solve. The approved substance stays: the cause of an extra review round lives in the reviewer's own report and PR comment (Step 0/Step 6 of `reviewer.md`), which the orchestrator already reads to apply each family's fix.
- **D2 — one added sentence**, in `run-wave` and `quickfix`, next to the family list: when a cause's fix belongs in a pocket-it file (`shared/implementing-common.md`, an agent template, a skill, `verify.sh`, a hook), that correction goes through a `/quickfix` on pocket-it — the orchestrator never edits pocket-it's own files directly. Closes the reviewer's D2 without inventing a new field or process.
- **D3 — removed the merged-tree verification** entirely from `reviewer.md` Step 3. It contradicted "never the full suite" in the same file, and with review groups capped at 3 PRs (F3), two PRs of the same wave in different groups are never on the same tree — exactly the base-drift case it was meant to catch. It's a real design problem on its own; the owner opens it as a separate task rather than patching it here.
- **D4 — reverted `.claude/skills/deps/SKILL.md`** to its original text (verified byte-identical to `main`, removed from `Files`). The direct `dependabot.yml` edit added in round 2 had the orchestrator implement a project change with no developer and no review, in the main session — exactly the anti-pattern the rest of this task removes elsewhere. No lane redesign added in its place; reverting is the minimal fix.
- **One straggler found while re-verifying**, not part of the reviewer's findings but the same class as F5: `README.md:110` ("the reviewer runs once per wave") — corrected in line with the already-approved `README.md:57` wording. No new text invented, same phrase reused.

## Acceptance criteria → verification (final)
- AC1: `run-wave` §4, group cap 3, reasoned from the measured ~20 turns/PR on `shared/lessons.md` (unchanged since round 2, approved).
- AC2: cause-first before any relaunch — dev blocker (`run-wave` §3) and red PR (`run-wave` §4 / `quickfix` §4) — applied per family, park only after analysis, never on a round count. The `cause`/`fix at` handoff is now entirely inside the reviewer's own report/comment (Step 0, Step 6 of `reviewer.md`); no separate log mechanism.
- AC3: `reviewer.md` Step 6 `ORCHESTRATOR:`/`HUMAN:` split; `retro.md`'s human exception says only that point waits.
- AC4: reread all six files in final scope (`run-wave`, `quickfix`, `reviewer.md`, `retro.md`, `CLAUDE.md`, `README.md`) whole; grepped for `user (decides|should|...)`, `goes to the user`, `say so and stop`, `one reviewer per wave`, `two fix rounds`, `relaunch a fresh`, `it recurs after`, `as a line the user`, `ROUND`, `merged-tree` — zero hits. `.claude/skills/deps/SKILL.md` reverted out of scope (verified identical to `main`).
- AC5: every remaining human-only stop still names the single point and continues the rest; unchanged by round 3.

No automated tests: prose/config files, as the task specifies. `bash bin/doctor.sh`: 0 errors, 0 warnings, 17 task files (board grew with PI-22/23/24/25 landing on `main` meanwhile; unrelated to this task).

## Deviations
Three mid-task corrections from the task owner, all recorded in the task file's Notes: (1) round 1→2, AC2 became cause-first-then-escalate instead of escalate-first; (2) round 1→2, scope widened to `CLAUDE.md`/`README.md`/`deps/SKILL.md` and to searching each finding's whole class, not just the cited line; (3) round 2→3, scope narrowed again — `ROUND` and the merged-tree test removed, the `deps` edit reverted, because round 2 had added mechanism without checking it against neighbouring rules, and the correct response to a second/third round is to look for a smaller diff, not a bigger one.

## Turns / stop
No stall, no budget overrun across all three rounds. Prose-only task, within the 120-turn budget throughout.
