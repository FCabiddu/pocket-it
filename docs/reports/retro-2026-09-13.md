# Retro 2026-09-13 — flow errors (targeted, not end of epic)

**Scope.** PRs #35–#64 opened 2026-09-12/13: 22 PRs read, 72 review rounds, 53 NEEDS WORK. 20 of 22 PRs had at least one NEEDS WORK (91 %), 3.3 rounds per PR. Two orchestrating sessions: average context 340k and 220k. Subagents: developer 56, reviewer 49, tech-architect 4, Explore 4, claude-code-guide 3, retro 2, planner 2, business-analyst 2, ux-ui-designer 1, general-purpose 0. Failure signatures: guard blocked 28, worktree isolation block 20, classifier denied 14, usage limit 11, API error 11, sleep blocked 3, AskUserQuestion in a subagent 2. Usage numbers come from `usage-report.py --days 2` on the orchestrating session's folder, which also holds other work.

**Trend.** This repo has no earlier retro PR, so there is no baseline. From the handoff log, day over day: NEEDS WORK 20 → 25, approved 8 → 11, example-not-class 3 → 10, BUDGET 0 → 2, STALL 0 → 0.

## Patterns and rules written
| # | Pattern (count) | Rule, where |
|---|---|---|
| 1 | Examples in place of the class: 16 NEEDS WORK tagged `example-not-class` across 10 PRs (rounds 7, 7, 5, 4). Every PR converged only once the test came from the declaration itself: one arity/type declaration per subcommand, every printed command executed on names with shell-special characters, the id regex compared file by file against a real board | `implementation-planner.md` Step 3: the class's dimensions plus a case table in the ACs from the start (prose tasks too), tests generated from the declaration. `implementing-common.md` §9 "Classes of cases and guards": test by construction. `developer.md` Step 5 cites it |
| 2 | Prescribed corrections not checked against the task's invariants: 4 measured. A recovery republished removed history twice (first on the base, then through a pushed rescue branch), a definition forbade an ordinary test, and a union was checked on the script's output rather than on the action taken from it. About 15 rounds in all where the reviewer's own earlier text was the gap | `reviewer.md` Step 4, extending the existing "executed before it goes in the comment" paragraph: check the output against every stated invariant, and the invariant wins over a demanded outcome |
| 3 | Orchestrator prompt errors: (a) narrowed scope, already covered, so no new rule; (b) an invariant written as a place ("not on the base"); (c) terms split across two PRs with no shared home; (d) an outcome ("back to green") that could only be reached by breaking an invariant | `run-wave` Step 4 "Writing a launch or resume prompt". `quickfix` Step 4 cites it by name and does not restate it |
| 4 | Guards with no threat model: 2. One was reframed in round 2 and closed in 4 rounds. The other had no model and was cancelled after 3 rounds, each finding a new shell form that executes text | `implementing-common.md` §9: the threat model goes at the top of every guard or check. `reviewer.md`: bypasses are judged against that model. `implementation-planner.md`: the threat model goes in the Goal |
| 5 | Agents resumed until their context was very high: 8 developers above 300k, the largest at 487k over 705 calls. `run-wave` had no rule for this | `run-wave` Step 4: from a PR's third developer round, launch a fresh developer on the existing branch instead of resuming the old one with SendMessage |
| — | Estimate for class-defining tasks | Fact in `docs/SESSION_HANDOFF.md`: such a task is M, never S, and its case table goes in the ACs |
| — | Stale compaction wording | `shared/lessons.md:17` (the orchestrator's "open a new session" step removed) and `CLAUDE.md` cost-rules row: compaction is automatic (`autoCompactWindow`) |

**PR.** Merged on its own with the audit prefix: see the PR linked in the handoff log line `retro 2026-09-13`. Checks: `testCommand` ran green on the branch (536 ok, 0 FAIL). `git grep` found 0 private names in the diff.

## Lessons (shared/lessons.md, 33 of 40)
- New, confirmed: specify a class by its dimensions and generate the test from the declaration. It already held on another project (reviewer.md Step 4 evidence).
- New, provisional: check a prescribed correction against the task's invariants.
- New, provisional: in prompts, write invariants as damage, never request an outcome that breaks an invariant, and give terms one home when a rule spans two PRs.
- New, provisional: a guard declares its threat model, and review judges against that model.
- New, provisional: from the third round, launch a fresh developer instead of resuming.
- Updated: line 17 (compaction). Removed: none.

## Proposed changes to pocket-it (human step — the private global CLAUDE.md, which this retro does not edit)
- "Due wave per sessione" paragraph: replace it with `- **Compattazione automatica.** Il runtime compatta da solo (autoCompactWindow): nessuna wave aspetta /compact, lo stato è su disco.` The current paragraph contradicts `run-wave` Step 5 and `lessons.md:17`.
- "Riprendere, mai rilanciare": append `Vale per un agente fermato a metà con lavoro suo; dal terzo giro di review sulla stessa PR si lancia un developer nuovo sul branch esistente (run-wave Step 4).`
- "Wave in parallelo" paragraph: append `I prompt di lancio e ripresa seguono run-wave Step 4 «Writing a launch or resume prompt»: invarianti come danno, nessun esito che violi un invariante, termini comuni in una sola PR citata per nome.`
- The confirmed lesson on classes is now written into `implementing-common.md` §9 and the planner in this pass. Once a human has checked those rules, delete the lesson line, per the file's own rules.

## Not actioned
- Duplicate facts (the force-push family repeated four times, the fail-closed hook twice). The facts section has 37 facts, below the hygiene threshold of 85.
- Reviewers skipping `verify.sh` because it builds its worktree under /tmp. PI-34 already covers this (open, NEEDS WORK).
- PI-14, PI-29 and PI-34 are still open. Their rounds are counted above, and fixing them is not this retro's job.

**Deviation, stated.** This retro edited pocket-it's agent templates and skills directly: the repo under retro is pocket-it itself, and the launch named those files as destinations. Only the private global CLAUDE.md is left for a human (above). No point needs the user.
