# PI-29 — round 5 (delta on the giro-4 review comment, PR #60)

## Scope
The 6 findings from the reviewer's giro-4 comment, and the coordinator's own correction to the
invariant it gave in the launch prompt (round 4's invariant named only "never write to
`refs/heads/<base>`" — too narrow; a pushed `rescue-<sha12>` branch republishes the very history a
deliberate rewrite may exist to remove). Corrected invariant: **no command `doctor.sh` prints ever
writes to origin, under any ref name.** All 6 findings closed.

## 1 — rewritten base, `save_cmd`
`bin/doctor.sh`: `save_cmd` no longer runs `git push origin {rescue_branch}`. It is now
`git fetch origin && git branch {rescue_branch} {prev}` — local only. Message says publishing the
rescue branch anywhere, and restoring the base, are decisions for a person; states explicitly "this
command never pushes".

## 2 — deleted base, normal run
Same fix: the printed command is `git branch {rescue_branch} {prev}`, no push.

## 3 — deleted base, `--accept-base`
Same fix in the `accept_base` branch's `status == "deleted"` case.

## 4 — `doctor.test.sh` reversed again
For each of the three printed commands: (a) static check that the extracted command text contains no
"push"; (b) the **entire** `git ls-remote origin` (not just `refs/heads/<base>`) is byte-identical
before/after, executed against a bare repo whose `pre-receive` hook rejects **every** write, not only
to the base (`install_reject_all_hook`); (c) a local `rescue-<sha12>` branch exists afterwards with the
lost commit. A mutation that appends `&& git push origin rescue-<sha12>` back onto each command proves
(b) is not vacuous (against a plain remote the ref set actually changes) and is independently rejected
by the reject-all remote.

## 5 — vacuous `base-seen` test
`BASE_SEEN_BEFORE` is now read **before** `--accept-base` runs (was read after, comparing the file with
itself). Added a mutation (built with a small Python text patch, not line-number `sed`, so it survives
future edits to the file) that makes `--accept-base` on a deleted base incorrectly call `_save_seen`
anyway; the "left untouched" check now goes red for it.

## 6 — untested "pruned" branch
Two new tests (normal run and `--accept-base`), both on a `base-seen` pointing at a sha computed with
`git hash-object --stdin` but never written (`-w` omitted) — guaranteed absent locally. Assert exit 1,
the "gone from the local object database" message, and that no `git branch`/push command is printed.

## Correction made during this round
Two round-3/round-4 facts in `docs/SESSION_HANDOFF.md` that prescribed pushing the rescue branch
("... pushed only under that new name") were removed; a round-4 fact already stating the correct,
broader invariant ("must not publish to origin, under any ref name") was left as the single source.

## Tests
`bash bin/doctor.test.sh`: 88 `ok`, 0 `FAIL`, exit 0.
Full `testCommand` (11 suites incl. `guard.test.sh`, `guard.heredoc.test.sh`, `install-live.test.sh`,
`status.test.sh`): exit 0, 0 FAIL.

## Note on guard.sh scope
Two assertions I initially wrote ("the guard blocks a push of the rescue branch") were wrong and
removed: the guard's own threat model only protects the base branch, so a push to `rescue-<sha12>` is
correctly *allowed* by guard — it is doctor's own job never to print such a command, which is exactly
what the ls-remote-diff and reject-all-hook checks measure instead.

## Turns
Well under budget (120). No `BUDGET`/`STALL` logged.

## Status
Task file left at `Needs Work` / PR #60 untouched (no status flip) — delta fix, reviewer re-reviews
with `Mode: delta`.

---

# Round 6 (delta on the giro-5 review comment)

## Finding → closed/how
`bin/doctor.test.sh:5` (`SCRIPT="$PWD/doctor.sh"`) compared against `doctor.sh`'s own `pwd -P`-resolved
`DOCTOR_ABS` in the "AC2 rewritten base: says how to accept ... absolute script path" assertion (line
294): wherever the checkout is reached through a symlink (macOS `/tmp` -> `/private/tmp`), `$PWD` keeps
the symlink component while `DOCTOR_ABS` resolves it, so the text mismatched → **closed**. Searched the
whole file for the class with `grep -n 'PWD\|pwd' bin/doctor.test.sh`: exactly two matches, `SCRIPT`
(the one that broke) and `GUARD` (not compared as text anywhere, but resolved too for consistency).
Both now built with `pwd -P` instead of a plain `pwd`/`$PWD`.

## Verification
- Reverted `SCRIPT` to the old `$PWD` form and ran the suite from `/tmp/pi29-symtest/bin` where
  `/tmp/pi29-symtest` is a symlink created for the test, pointing at `/tmp/pi29-symtest-real`:
  reproduces the exact reported failure, `FAIL AC2 rewritten base: says how to accept an intentional
  rewrite, absolute script path`, exit 1.
- Same run with the fix in place: 88 `ok`, 0 `FAIL`, exit 0.
- Full suite also re-run from the worktree (a plain, non-symlinked path): 88 `ok`, 0 `FAIL`, exit 0.
- Full `testCommand` (11 suites) from the worktree: exit 0, 0 FAIL.

## Turns
Well under budget. No `BUDGET`/`STALL`.

## Status
Task file left at `Needs Work` / PR #60 untouched — delta fix, reviewer re-reviews with `Mode: delta`.
