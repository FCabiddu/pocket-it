# PI-19 — report

## What changed and why
Rewrote the "Session hygiene" bullet in `.claude/skills/run-wave/SKILL.md` (point 5, "Close the wave"). The old text ended the report with a question ("`/compact` (o una sessione nuova)") that handed the compaction call to the user. The user has less to judge with than the orchestrator — it never saw how many agents were launched, how many review rounds ran, how much re-reading happened. The rewrite keeps the decision with the orchestrator, states the constraint that neither the orchestrator nor any tool in this skill can start a compaction or read context usage (that belongs to the runtime), and lists the accountable signals already on disk (`wave … closed` lines, agents launched, NEEDS WORK/delta-review rounds) as the basis for the call. After the second closed wave the report now ends with a statement, not a question.

## Acceptance criteria → verification
- AC1 (decision attributed to orchestrator, not phrased as a question) — verified by re-reading the bullet: "The call to compact is yours, not the user's — it is not a question to put to them." The closing message is a statement ("... la sessione va compattata ...", no question mark).
- AC2 (execution is explicitly not the model's) — verified: "no tool, hook or command in this skill starts a compaction, and the running session cannot read its own context usage — that stays with the runtime, never with you."
- AC3 (accountable signals, not an unreadable context threshold) — verified: the bullet lists `wave … closed` lines logged today, agents launched across them, and NEEDS WORK/delta-review rounds; no context-percentage or token-count threshold appears.
- AC4 (no other phrase in the skill hands the same decision to the user) — verified with `grep -n -i "compact" .claude/skills/run-wave/SKILL.md`: exactly one occurrence, the rewritten bullet. The only other "user decides" phrase in the file (point 1, blocked-task list) is a different decision, unrelated to compaction.

## Tests
None — prose-only task, no code or automated tests. Verification was a full re-read of the file plus the grep above (see AC4).

## Deviations
None. Did not touch the private orchestrator entry point (out of scope for this repo, and the task explicitly says not to look for or cite it).

## Checks run
- Full re-read of `.claude/skills/run-wave/SKILL.md` (41 lines) to confirm no contradiction with the rest of the skill.
- `grep -n -i "compact"` on the file — single match.

## Turns / budget
Well under the 60-turn budget. No `BUDGET` or `STALL` logged.
