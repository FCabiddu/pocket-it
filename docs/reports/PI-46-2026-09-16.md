# PI-46 — Derive package-script names in the verify sandbox, and name the borrowed-node_modules limit

## What changed and why
Two loose ends left open by PI-41, both "a check that can pass while proving nothing":

**(a) `bin/verify.sh`** — the "Not covered, deliberately" comment on `base_blockers` (top of the function,
:139) now names the branch/base worktrees' shared `node_modules` (symlinked from the main checkout at :112
and :279) as a third thing no exit code can settle: a branch check that writes into it changes what the
base re-run reads, so exit 3 ("inherited") can be reported for a reason that is not the base's own. This is
a sentence, not a guard — the task explicitly excludes buying the limit out (a full install per run) and
directs naming it instead; `reviewer.md`'s existing hand-re-run rule already covers the vector.

**(b) `bin/verify.test.sh`** — the `full`/`marked` sandbox package scripts (`lint`, `type-check`,
`typecheck`, `test:affected`) were hand-typed at line 528. They are now **derived** from the same
`# --- command producers` block the test already reads mechanically for paths and binaries (PI-41 F1). A
new `scripts_named()` helper reuses `base_blockers`' own `shlex`/pm/pmrun state machine and, critically, its
own `PMSUB` list of package-manager subcommands (read out of `verify.sh` with `grep`/`sed`, never retyped)
so the list stays in one home. `full`'s `package.json` is now built from the union of every producer's
derived script names; `marked`'s stays the explicit `{"scripts":{}}` it already was.

## Acceptance criteria -> tests
- AC1 (scripts derived, not typed) -> `full`'s package.json built from `FULLSCRIPTS`, itself built by
  running `scripts_named()` over every literal producer template through `expand()`. Verified: static
  script set now excludes the unused `"test"` entry that was in the old hand-typed list but named by no
  producer — proof the old list was typed, not derived.
- AC2 (whole class of invocation shapes, never observed examples) -> two new table-driven assertions:
  "every package manager and every accepted run-shape yields the script, never a flag around it" (4 pms x
  3 shapes x flag-before/flag-after = 24 cases) and "every package-manager subcommand on PMSUB is excluded,
  whatever manager it follows" (PMSUB's own word list x 4 pms). The case list for the second is read from
  `base_blockers`' own `pmsub`, not re-typed — see "One rule, one home" below.
- AC3 (marked still excludes the derived scripts, proven by execution) -> "PI-46 AC3 mutation" temporarily
  writes one derived script into `marked/package.json` and shows `base_blockers` **wrongly clears** the
  check (empty result = "safe to re-run here"), then restores the file and "PI-46 AC3" shows the correct,
  unmutated answer (non-empty = blocked). The mutation is executed, not reasoned about.
- AC4 (name the node_modules limit) -> comment edit in `bin/verify.sh`, prose only, no test needed (task
  says so explicitly).
- AC5 (suite green, assertion count not lower) -> verified below.

## One rule, one home (AC2 note)
Reused `base_blockers`' `pmsub` list rather than duplicating it: `PMSUB=$(grep -o 'local pmsub=" [^"]*"'
"$SCRIPT" | sed -E 's/^local pmsub="//; s/"\$//' | head -1)` extracts the exact string value out of
`verify.sh`'s source, byte-for-byte (including its leading/trailing space, which is how the `*" $tok "*`
substring match works both there and in the new `scripts_named()`). A subcommand added to that list in
`verify.sh` is excluded by `scripts_named()` too, with no edit to the test file.

## Mutation executed for AC3 (measured)
Before restoring `marked/package.json`: `base_blockers "npm run lint" package.json` against the mutated
tree (with `lint` wrongly declared) returned empty (cleared — wrong). After restoring: the same call
against the correct, script-less `marked` tree returned a non-empty reason (blocked — correct). Both are
now their own `ok` assertions ("PI-46 AC3 mutation" and "PI-46 AC3").

## Changes
- `bin/verify.sh` — extended the `base_blockers` "Not covered, deliberately" comment with the shared
  `node_modules` case (AC4). No behaviour change.
- `bin/verify.test.sh` — added `PMSUB`, `scripts_named()`; moved `expand()`/`PRODBLOCK` earlier so the
  `full` sandbox's `package.json` can be built from derived scripts instead of a literal; added the AC1
  script-derivation check, the AC3 mutation pair, and the AC2 class-construction pair (5 new assertions).
- `tasks/PI-46-verify-sandbox-derives-script-names.md` — status/branch/PR fields (uncommitted per the
  close protocol — left for the orchestrator).
- `docs/SESSION_HANDOFF.md` — not touched here (throwaway rule does not apply; see Handoff below), left
  uncommitted per the close protocol.

## Checks run
- `bash -n bin/verify.sh && bash -n bin/verify.test.sh` — both syntax-clean.
- `bash bin/verify.test.sh` — full suite, tail: `verify.test.sh: ALL PASS`.
- Assertion count (AC5): ran the pre-change `bin/verify.test.sh` (from `origin/main`) in place against the
  current `bin/verify.sh` — 138 `ok` lines. The new file, same run — 143 `ok` lines. Not lower; five more
  (AC1 x1, AC3 x2, AC2 x2).
- `git diff --name-only $(git merge-base origin/main HEAD) HEAD` — exactly `bin/verify.sh` and
  `bin/verify.test.sh`, matching the task's `**Files**:` and the concurrent-agents exclusion list (did not
  touch or run `bin/handoff.sh`, `bin/handoff.test.sh`, `bin/doctor.test.sh`, `bin/cleanup-merged.sh`,
  `bin/cleanup-merged.test.sh`).

## Notes / deviations
The old hand-typed sandbox script set included a `"test"` script that no current producer template names
(the real `test` script, if any, only ever comes from the dynamic `testCommand` config value, which is the
one producer this file already treats as non-literal and covers end-to-end separately, F1a/F1b/F1c). It is
correctly absent from the derived set — nothing in the producer block names it literally, so nothing should
declare it in `full` either; keeping it would have been the same defect the task describes, just on the
positive side.

## Handoff
No fact worth adding beyond what is already in the project's memory (PI-41's exit-3/shared-node_modules
facts already cover this file's invariants); this task only closes two loose ends those facts already
describe. Nothing written to `docs/SESSION_HANDOFF.md`.

Effort: 14 min elapsed (budget 120 min) — wt:task-pi-46-verify-sandbox-script-names

## Resume — PR #88 review findings (2026-09-17)

Reviewer labelled `needs-work`, cause `first-round`, 3 findings. AC1/AC2/AC3/AC5 were confirmed
already met and left untouched. Fixed in `32809e3`:

- **F1 — closed — 32809e3.** The AC2 negative loop iterated `$PMSUB`, the same live value
  `scripts_named()` reads out of `verify.sh` — shrinking or emptying the real `pmsub` shrank/emptied
  the test's own loop input too, so it passed vacuously (measured: emptying `pmsub` entirely still
  gave `ALL PASS`, 143 ok). Fix: added `PMSUB_FLOOR`, a hardcoded, test-owned, independent list
  (same 29 words as today's `pmsub`), plus a new assertion that live `PMSUB` is a superset of
  `PMSUB_FLOOR`; changed the AC2 negative loop to iterate `PMSUB_FLOOR` instead of `PMSUB`. Verified
  by mutation: removing `exec dlx` from `verify.sh`'s `pmsub`, and separately emptying it entirely —
  both times the new F1 superset assertion and the AC2-floor assertion went FAIL; both mutations
  reverted, confirmed clean.
- **F3 — closed — 32809e3.** The producer selector `CMD_[A-Z]+=` (used identically in the new
  derivation loop and the pre-existing PI-41 loop, 5 occurrences) silently drops any producer name
  containing a digit (reviewer's probe: `CMD_RV1`) from both the sandbox and the assertions at once.
  Fix: widened all 5 occurrences to `CMD_[A-Z0-9]+=`; added an independent ground-truth cross-check
  — a Python regex over `\w+` (letters, digits, underscore), filtering `_NEEDS` lines and empty
  initializers by name/content rather than by character-class boundary — asserting the bash-side
  `NPROD` count equals the Python-side count. Verified by mutation: inserting
  `CMD_RV1="$PM run banana"` is now picked up directly by the widened selector (11==11, no
  cross-check failure — confirms the direct fix covers the reviewer's exact probe); inserting
  `CMD_E2E_RUN="$PM run banana"` (an underscore, still outside even the widened class) is missed by
  the selector but caught by the cross-check (10 != 11, FAIL) — proving the defense-in-depth catches
  narrowings the widened class itself still misses. Both probes reverted, confirmed removed.
- **F2 — closed — 32809e3.** The AC4 comment cited absolute line numbers (`:111 and :279`), already
  stale after my own AC4 edit shifted them to `:112`/`:282`. Fix: dropped the line-number citation,
  described the invariant structurally instead ("the main checkout's own copy, symlinked into both
  trees rather than installed fresh in each"), matching every other entry in that comment list. No
  test needed — prose only, as with the original AC4.

Out of scope, per the coordinator: `pmsub`'s pre-existing over-broad clearing of bare `<pm> <script>`
forms against a scriptless tree is PI-41 behaviour, not this task's, and is getting its own task.

Checks re-run after the fixes: `bash -n bin/verify.sh && bash -n bin/verify.test.sh` clean;
`bash bin/verify.test.sh` — `ALL PASS`, 145 `ok` lines (up from 143, +2 for F1's superset assertion
and F3's cross-check assertion — AC5 still holds, not lower). Scope re-confirmed via
`git diff --name-only $(git merge-base origin/main HEAD) HEAD`: `bin/verify.sh`,
`bin/verify.test.sh`, this report — no touch to any concurrent-agent file.

Effort (resume): ~9 min elapsed — wt:task-pi-46-verify-sandbox-script-names
