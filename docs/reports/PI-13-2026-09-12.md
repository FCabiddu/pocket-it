# PI-13 — guard fail-open on any assignment/prefix in front of `git push`

## Defect
The push classifier in `.claude/hooks/guard.sh` anchored on `git` at the start of each
segment, so anything in front of `git` made it miss and the command was never classified —
the guard did not deny, it did not see. Fail-open spellings that reached the base branch:
a leading variable/`env` assignment (`FOO=bar git push`), the one-off-config form
(`git -c k=v push`), the audit prefix with a NON-authorized value (`=0`/`=2`, a form
declaring itself unauthorized yet disarming the check), plus two same-bucket forms that
reach the base without naming it — a glob refspec and `--all`.

## Fix (`.claude/hooks/guard.sh`)
- Gate fires whenever the stripped command has `git` and `push` as bare words; the
  classifier returns empty (ALLOW) for non-push git commands, and STR/heredoc stripping
  still keeps a mere mention out of it.
- Replaced the `^git` regex with a tokenized parse that strips leading `env`/`NAME=VALUE`
  prefixes (audit prefix included, any value) then consumes git globals, enumerating the
  value-taking ones (`-c -C --namespace --git-dir --work-tree --exec-path --super-prefix
  --config-env`) so a config value is never read as the subcommand. Authorization stays in
  the shell by EXACT equality on `=1`; the parser only classifies the target.
- `--all` and glob-destination refspecs are treated as base-reaching (`is_glob` +
  `fnmatch`-based `targets_base`, so `refs/heads/*` matches main while `refs/heads/task/*`
  does not — task cleanup keeps passing).
- Classifier is fed the RAW command, so a quoted glob refspec is seen, not collapsed to STR.

## Tests <-> criteria (`.claude/hooks/guard.test.sh`, new PI-13 section, 113/113 ok)
One+ case per AC: AC1 var/env prefix; AC2 `-c` forms; AC3 `=0/=2/=10`; AC4 exact `=1` passes,
prefixed force still blocks; AC5 same spellings to a feature branch pass; AC6 glob quoted &
unquoted, `+`/plain/single-ref; AC7 `--all` with force and bare; AC8 task cleanup in the same
spellings; controls (`git config push.default`, `git log`) that mention push but are not one.

## Mutation proofs (each reverts exactly its own rows to red, verified)
Re-anchor to `^git` → AC1+AC3 red (the anchor the task named); `value_opts=set()` → AC2 red;
`targets_base` equality-only → AC6 red; `has_all=False` → AC7 red.

## Checks
Full testCommand chain (cleanup-merged, worktree, doctor, handoff, next-wave, guard,
guard.heredoc, verify) — all green. No new test file → no `.pocket-it.json` change.
Shell explicit-main greps and PI-10 force/delete/mirror/prune logic unchanged and still green.

## Incident (not caused by the change)
The worktree was removed mid-task by the merged-worktree cleanup (a branch with no own commit
reads as merged). Work was reapplied to a fresh worktree and a WIP commit made at once so the
branch owns a commit; a stray edit that briefly hit the main checkout's active hook was
reverted to HEAD. Within budget (S/120); no STALL.

---

## Round 2 — review of PR #47 (9 findings), threat model reframed
The coordinator narrowed scope: the hook guards a COOPERATIVE agent from a base-branch push
written in a NORMAL form; deliberate evasions are out of scope and left to server-side branch
protection. Changes this round:

- **F1 (regression, priority)** — the classifier split the RAW command with a regex on the
  raw text, which cut inside quotes and heredoc bodies: a commit message or PR body holding
  `;`/`|`/`&&` plus push-shaped words became a fake push segment and blocked a legit command.
  Replaced with a quote-aware `shlex` tokenizer (heredoc bodies removed first), splitting on
  operator tokens only. A lexer error (unbalanced quotes) now yields ALLOW.
- **F8** — authorization moved fully into the classifier and is now per-push: the exact prefix
  authorizes only on that push's own command word. Removed the shell-level `AUTHORIZED_PUSH`
  greps (whole-command, the F8 defect at old line 45); the classifier is the single authority.
- **F5** — every refspec after the remote is classified (not only the first), and value-taking
  push options (`-o/--push-option/--receive-pack/--exec/--repo`) consume their value.
- **F6** — destinations that reach the base without being named `main`: `heads/main` (DWIM),
  the matching refspec `:`/`+:`, and `@` (HEAD).
- **Threat model** written at the top of `guard.sh`: what it covers, what it deliberately does
  not (eval/`bash -c`/`env -i`/wrappers/quoted-escaped git/aliases/`send-pack`/push-affecting
  config/newline-separated commands), and that the real boundary is server-side.
- **F9** — the handoff fact rewritten to say the hook covers mistakes, not evasions; the stale
  line-8 fail-open fact marked closed by PI-13.

Tests (`guard.test.sh`, PI-13 round-2 section, 140/140 ok): F1 six regression ALLOWs; F8 two
BLOCKs; F5 multi-refspec + value-opt BLOCKs; F6 `heads/main`/`:`/`+:`/`@` BLOCKs; controls
(task multi-refspec, `:task/old`, `-o … task/x`, `@` from a feature branch) ALLOW.
Mutation-proven: undo tokenizer → F1 red; whole-command auth → exactly the two F8 rows red;
only-first-refspec → F5 red; narrow `norm()` → `heads/main` red; drop `:`/`@` → those red.
Full testCommand chain green.
