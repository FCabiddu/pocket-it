# Review PI-30 — PR #53, delta del giro 2 — APPROVED

**Reviewer**: reviewer · **Modo**: delta (`a4be904..d7bb364`) · **CI**: nessuna (`pipeline: false`) · **Merge**: orchestrator (default)

## Conflitti
- `main` è avanzato (PI-25), e `docs/SESSION_HANDOFF*.md` era in conflitto.
- Risolto per unione: la versione di `main`, più 3 fatti e 3 righe di log di PI-30, 0 righe perse. Commit locale `b289a38`.
- **Il push è stato negato dal sistema di permessi della sessione del reviewer.** Deve pusharlo l'orchestratore, oppure rifare il merge.

## Test eseguiti (`testCommand` completo)
- PI-30 + main: rc 0, 427 ok, 0 FAIL.
- PI-30 + main + PI-32 sovrapposte: rc 0, 496 ok, 0 FAIL. Il conflitto era solo nei file di handoff.

## Finding del giro 1
1. **Fail-open durante l'aggiornamento: chiuso.**
   - Clone accanto alla copia, controllo della risposta del guard (2/0), swap atomico senza fallback.
   - Mutazione «controllo del guard tolto»: exit 1, 5 FAIL.
   - Mutazione «swap in tre `mv`»: exit 1, 5 hook run senza exit 2 e 1227 assenze.
   - Hook `|| exit 2` presente nei passi 2, 3 e 6. Il rollback non dipende più da `$LIVE`.
2. **`.git` linkato al passo 5: chiuso.**
3. **Nomi del checkout: chiuso.** Chiuso con `expandvars`, `FLAG` per `bash -c`, il passo 5b e il passo 6 senza `ALL OK` finché restano nomi.
- **Uscita da un lock-out senza Bash:** presente. Si modifica `settings.json` con Edit, si apre una nuova sessione e si lancia `--rebuild`.

## Il test del README esegue il README: mutazioni sul README, tutte rosse
- passo 5 con `.git`: 3 FAIL
- passo 3 senza `|| exit 2`: 6 FAIL
- rollback con `[ -e "$LIVE" ]`: 1 FAIL (exit 1)
- passo 6 senza la sonda del guard mancante: 2 FAIL
- passo 4 no-op: 2 FAIL
- passo 6 con `ALL OK` e nomi residui: 1 FAIL
- README non mutato: 24 ok

## Non bloccanti
- Il corpo della PR è fermo al giro 1 («rewritten in place… not in this PR») e non giustifica `install-live.readme.test.sh`, che è fuori da Files. Va aggiornato prima dello squash.
- Un kill durante l'installazione lascia `<dest>.next.<pid>`, e nessun passo lo ripulisce.
- Il lock-out causato da `python3` mancante non si risolve con `--rebuild`, perché il nuovo guard non risponde. Il testo non lo dice.

## Etichette
`approved` aggiunta, `needs-work` rimossa, PR marcata ready.
