# PI-6 — Align retro thresholds and read budget to the 100-fact cap

## What changed and why
PI-5 raised the handoff fact cap from 30 to 100 in `bin/handoff.sh` (`CAP=100`, confirmed
before editing). Three prose lines elsewhere still assumed 30 and contradicted it:

- `.claude/agents/retro.md:48` — "Keep the section ≤ 30 lines" → "≤ 100 lines".
- `.claude/agents/retro.md:58` — facts-hygiene paragraph: cap (30 → 100), trigger
  (≥ 25 → ≥ 85), target (≤ 20 → ≤ 70). Kept the old proportions at the new cap (83%
  trigger, 67% target), as the task's Notes specified. Everything else in the
  paragraph (what moves to best-practices, what gets deleted, listing every
  moved/deleted fact in the report) untouched.
- `.claude/agents/shared/implementing-common.md:39` — "at most 30 lines" → "the whole
  facts section, up to the cap of 100 one-line facts". The `awk` command on that line
  is byte-for-byte unchanged.

## Tests ↔ criteria
- AC1 — retro.md:48 now reads "≤ 100 lines", "replace a fact now covered by a rule"
  clause untouched.
- AC2 — retro.md:58 now reads cap 100, trigger ≥ 85, target ≤ 70; rest of paragraph
  unchanged (verified by diff, only the three numbers changed).
- AC3 — implementing-common.md:39 read budget updated, awk command unchanged (verified
  by diff).
- AC4 — `grep -rn "30" .claude/agents/retro.md .claude/agents/shared/implementing-common.md`
  returns 5 hits, none about the facts cap: the "older than 30 days" stable-fact rule
  (retro.md:58, unrelated — a date, not a count, per the task's own carve-out), port
  3100, the Estimate table (XS 60 · S 120 · M 200 · L 300 — no literal "30"), and the
  "~30 turns" stall threshold. None is the facts cap.
- AC5 — full `testCommand` run (10 test files) green, no new tests: these are agent
  prose files, not code, matching the task's "Tests expected" section.

## Deviations
None. No file outside the two named in `**Files**:` was touched. `bin/handoff.sh` was
read only to confirm `CAP=100`, not edited, as instructed.

## Checks run
`bash bin/cleanup-merged.test.sh && bash bin/worktree.test.sh && bash bin/doctor.test.sh
&& bash bin/handoff.test.sh && bash bin/next-wave.test.sh && bash .claude/hooks/guard.test.sh
&& bash .claude/hooks/guard.heredoc.test.sh && bash bin/verify.test.sh &&
bash bin/install-live.test.sh && bash bin/install-live.readme.test.sh` — all green
(only `ok` lines; `ERROR`/`not ok` strings seen in the output are expected fixture
output inside doctor's own duplicate-id tests, not failures).

## Turns
Well under the 60-turn budget (XS task, two-line prose edits, no BUDGET/STALL logged).

## Round 2 (review finding)
Reviewer (round 1) found `.claude/agents/developer.md:32` still reading the facts with
"shared rules §3, ≤ 30 lines" — the class the task exists to fix (any instruction that
caps the facts read at 30), not just the two files literally named. Fixed with the same
wording as `implementing-common.md:39`: "the whole facts section, up to the cap of 100
one-line facts". Outside `**Files**:` — justified above and in the commit.

Then traced the whole class with `grep -rnE "30 (lines|righe|facts|fatti)|≤ ?30|at most 30|max 30" .claude/ bin/ README.md`.
Occurrences found and disposition:
- `.claude/agents/developer.md:32` — facts read budget → fixed (this round).
- `.claude/agents/retro.md:48,58` — fixed round 1.
- `.claude/agents/shared/implementing-common.md:39` — fixed round 1.
- `.claude/agents/documentation-agent.md:75` — branch slug length cap, unrelated to facts. Left as is.
- `.claude/agents/implementation-planner.md:31` — scope table (`full` → `≤ 300`), unrelated to facts. Left as is.
- `bin/handoff.sh:22,27` and `bin/handoff.test.sh:4,67,78,84` — the script's own "max 30 righe" template comment and the test proving it self-normalises to `CAP` on every run. Left as is: the task forbids touching `bin/handoff.sh`, and the reviewer confirmed the comment is not a real limit — it is overwritten to the live cap on every `handoff.sh` invocation.

Re-ran the full `testCommand` (10 files) individually after the fix: all exit 0, zero
`not ok` lines each.

## Notes for the next agent
PI-33 depends on this same file pair — diff kept as small as the finding allowed
(retro.md, implementing-common.md, developer.md; one line each beyond round 1), so
PI-33's diff should mostly not conflict.
