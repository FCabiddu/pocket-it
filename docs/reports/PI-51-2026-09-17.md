# PI-51 — Reinstall the shared checkout after a lockfile merge, and fail verify on a stale install

**Label**: DevOps · **Risk**: high · **Budget**: 200 · **Branch**: `task/pi-51-reinstall-shared-checkout`

## The invariant this implements

No verification may pass while the installed tree disagrees with the lockfile, and no reinstall may happen
while anything is running against the shared checkout. Both halves are stated as the harm, in the code and in
the skills: *a verdict about code that is not installed is not a verdict*, and *no reinstall while anything is
running against this checkout — it would swap the installed code under a run in progress and corrupt what
that run measured*.

## What changed

### `bin/install-drift.sh` (new) — the whole mechanism, one script, two questions

* `check [dir]` — compares the installed `node_modules` with the lockfile. Exit **0** match (or nothing to
  compare), **1** drift, **2** usage, **3** no verdict possible. It prints, per drifted entry,
  `drift  <name> — installed X, locked Y (<path>)`, and a summary that always states **how many entries it
  compared**, so a clean answer can never mean "nothing was read".
* `reinstall [dir]` — decides whether a reinstall is needed (effect first: the drift check; event second:
  `--since <ref>` over the lockfile names, used only where there is no drift check for that package manager),
  then whether it may run now, then runs the lockfile's own frozen install. Exit **0** done or nothing to do,
  **1** drift persisted / install failed / package manager absent, **3** cannot tell, **4** deferred.
* Flags: `--since <ref>`, `--except <branch|slug>` (repeatable — the merged PR's own worktree), `--running
  "<what>"` (the caller's knowledge that it launched agents; processes are out of the script's model on
  purpose, which is exactly why this flag exists), `--dry-run`, `--no-log`.

Threat model, written at the top of the script and honoured in every branch — **never a silent pass**:

| dimension | what the script does |
|---|---|
| scoped `@scope/name` | compared like any other; the path is split on the last `node_modules/`, so the scope stays part of the name |
| transitive-only / nested (`node_modules/a/node_modules/b`) | compared; npm v2/v3 `packages` paths and v1 recursive `dependencies` both |
| npm lockfile v1 / v2 / v3, `npm-shrinkwrap.json` | read; v2 is read once, from `packages` |
| a lockfile shape it does not understand | `NOT SUPPORTED — … declares neither "packages" nor "dependencies" (lockfileVersion N)`, exit 3 |
| a lockfile it cannot parse | `NOT SUPPORTED — … cannot be parsed`, exit 3 |
| pnpm / yarn / bun | `NOT SUPPORTED — drift check not supported for <pm> (<lock>)`, exit 3 — stated, never clean |
| two lockfiles in one tree | the readable one is compared **and** the other is declared uncompared in the same run |
| a package installed at an unreadable version | drift (`installed version unreadable`), not a pass |
| a package the lockfile requires and the tree lacks | drift (`locked Y, not installed`) |
| optional / platform-restricted absent, workspace `link`, an entirely absent dev set | not drift — and each is counted out loud (`N not comparable, M legitimately absent`, `all N dev entries … are absent`) |
| `node_modules` borrowed through a symlink | followed on purpose: that borrowed tree is the one being measured |

Deferral is conservative by construction: any worktree of the checkout under `.claude/worktrees/` or
`.worktrees/` defers, `--running` defers, and **a worktree list that could not be read defers too** ("an
unread list is never an empty one"). A false defer costs a printed line; a false proceed corrupts a running
measurement.

### `bin/verify.sh` — the guard

* After the borrowed-`node_modules` block and **before the first check runs**: `install-drift.sh check .`.
  Drift ⇒ `FAIL  install drift …` plus the drifted entries, where the borrowed tree came from
  (`node_modules is borrowed from <path>`), a fix that is runnable as printed (`install-drift.sh reinstall
  <the shared checkout>` — not the throwaway worktree, which is deleted seconds later), and
  `verify: could not run — the installed tree disagrees with the lockfile (nothing was measured)`, **exit 2**.
  Exit 2, not 1, keeps PI-41's meaning intact: a stale install is not the branch's defect, and nothing valid
  was measured. `NOT SUPPORTED` becomes a `warn` line and the run continues.
* Fail-closed on its own absence: a `verify.sh` without `install-drift.sh` next to it exits 2 rather than
  skipping the guard.
* The same rule on the base side of the PI-41 attribution: a base worktree whose borrowed install is not its
  own lockfile's cannot answer "does this check fail at the base too", so the attribution goes to **unknown**
  (with the reason and the reinstall command), never to "pre-existing on base".

### `.claude/skills/quickfix/SKILL.md` (Step 4) and `.claude/skills/run-wave/SKILL.md` (Step 5)

After the merge and the `git pull --ff-only`, both close by running `install-drift.sh reinstall .` on the
shared checkout, with `--running "{what you launched}"` whenever a developer, reviewer or QA agent is still
working against it; both state the harm rather than a place; both report the script's last line; both say that
exit 4 is a deferral already written to the handoff log with `handoff.sh log`, to be re-run once those agents
report, and that exit 3 is re-run with `--since {the base commit}`.

### Bookkeeping

`.pocket-it.json` `testCommand` now chains `bash bin/install-drift.test.sh` (a suite not in `testCommand`
runs once and never again). `CLAUDE.md`'s script table gained the `install-drift.sh` row and the extended
meaning of `verify.sh`'s exit 2.

## Acceptance criteria → tests

| criterion | covered by |
|---|---|
| **AC1** merged PR touching any lockfile → the lockfile's own frozen install in the main checkout, reported | `install-drift.test.sh`: "every declared lockfile reinstalls with its own package manager's frozen install" (loops the script's `LOCKFILES` declaration against this suite's own command table: `npm ci`, `pnpm install --frozen-lockfile`, `yarn install --frozen-lockfile`, `--immutable` for berry, `bun install --frozen-lockfile`), "a drifted checkout with nothing running is reinstalled", "it says which command it ran and where", "it reports the reinstall in a closing line", "the install command really was invoked, in the checkout", "and the tree now matches the lockfile", "a second run on an already-matching tree reinstalls nothing", "with --since, a merge that touched the lockfile is what triggers the reinstall", "a merge that touched no lockfile reinstalls nothing"; `verify.test.sh`: "quickfix / run-wave reinstalls the shared checkout after a merge" |
| **AC2** another agent still running → no reinstall, deferral logged, skill says to re-run | `install-drift.test.sh`: "a worktree in use defers the reinstall, exit 4", "the reason names the harm, not the place", "and names what is running", "nothing was installed", "the tree is left drifted, exactly as it was", "the command to run once they report is printed in full", "the deferral is written to the handoff log", "the merged PR's own worktree is excluded by name and the reinstall proceeds", "a caller that knows it launched agents defers with --running", "with no way to read what is running, the reinstall is deferred, not risked"; `verify.test.sh`: "quickfix / run-wave states the harm the deferral avoids, not a place" and "leaves the deferred reinstall in the handoff log" |
| **AC3** a drifted install fails `verify.sh` on the branch worktree **and** on the symlinked-from-main path, naming package, installed, locked, command | `verify.test.sh`: the eight "PI-51 AC3 —" assertions on the branch worktree (exit 2, the reason, `react — installed 18.2.0, locked 18.3.1`, the fix naming the shared checkout, where the tree came from, "nothing was measured", no verdict of any kind, not one check executed) and the six "PI-51 AC3 base —" ones on the base worktree of the attribution (exit 1, attribution refused and why, the package named, the fix, never "pre-existing", the branch's red still reported). The class of lockfiles: `install-drift.test.sh` "every declared lockfile either gives a verdict or refuses by name, none passes in silence", plus v1/v2/v3, shrinkwrap, unparsable, unknown shape, two lockfiles at once |
| **AC4** matching install, or no lockfile / no `node_modules` → unchanged | `verify.test.sh`: "with the install matching the lockfile the same branch runs and exits 0", "a matching install is verified as before, GREEN", "and it says what it compared", "the checks really did run this time", "a lockfile with nothing installed anywhere is not drift", "a repository with no lockfile at all is untouched by the guard", "and the guard says nothing about a project it has nothing to check"; `install-drift.test.sh`: the four AC4 assertions and the three usage ones |
| **AC5** mutation-proven, both directions | `install-drift.test.sh`: mutation 1 — the comparison line replaced by `if False:` in a **copy** of the script: the drifted fixture now exits 0 and says `OK — 4 compared entries` (it ran, it did not crash into a false green). Mutation 2 — `if name.startswith("@"): continue` inserted at the marked site: the scoped fixture goes to `OK — 3 compared entries` while the unscoped one **stays red**, so the mutation is the scoped dimension alone. Both mutants are asserted to be whole scripts that produced a positive signal. The two sites are marked `# MUTATION SITE …` in the script so the proof stays executable |

Class-by-construction and anti-vacuity: the lockfile loops read the script's own `LOCKFILES` declaration at
run time (a new package manager is covered without touching the suite) and are held above a floor list that
belongs to the suite (a declaration that shrank cannot silently empty the loops); every `OK` assertion also
asserts the **number of entries compared**, so "clean because nothing was read" fails; a package manager the
declaration grows but the suite's command table does not know fails with "add it, do not skip it"; the
verify-level pairs are world mutations — one byte of an installed `package.json` — run in both directions.

## Defects the suite found in the implementation

1. **The deferral never fired.** `live_worktrees()` piped `git worktree list --porcelain` into `python3 - …
   <<'PY'`: the pipe and the heredoc both claim stdin, the pipe won, and python tried to execute the git
   output as its program. The reader silently produced nothing, i.e. "no worktree is running", which is
   precisely the wrong answer for this guard. Fixed by passing the porcelain in the environment
   (`WT_PORCELAIN`), and hardened: a reader that exits non-zero now defers instead of proceeding.
2. `--since` built its reason line with `printf '%s'` before `tr '\n' ' '`, so a single changed lockfile came
   out as `pnpm-lock.yamlchanged since …`.
3. `verify.sh` printed install-drift's own `run \`npm ci\` in <dir>` line, which named the throwaway worktree
   about to be deleted — a command that is not runnable as printed. It now prints the borrowed tree's origin
   and a fix aimed at the shared checkout.

## Checks

```
bash bin/install-drift.test.sh      → install-drift.test.sh: ALL PASS   (90 assertions)
bash bin/verify.test.sh             → verify.test.sh: ALL PASS          (178 assertions, 33 of them new, PI-51)
bash bin/doctor.sh                  → doctor: 0 error(s), 0 warning(s), 52 task file(s)
the whole testCommand chain, suite by suite → exit 0 for all 13 suites
```

## Notes, deviations and what is deliberately not covered

* `bin/install-drift.sh` and `bin/install-drift.test.sh` are new files; the task's `**Files**` line
  anticipated them. No other file outside that line was touched.
* Not covered, deliberately and stated in the code: the **contents** of an installed package (a package
  edited in place at the version it declares), and drift checking for pnpm/yarn/bun — those produce an
  explicit `NOT SUPPORTED` line and a `warn` in `verify.sh`, because refusing every pnpm/yarn/bun project
  outright would buy nothing anyone can act on. `reinstall` still works for all of them.
* Liveness is read from worktrees and from what the caller declares with `--running`; **processes are not
  inspected**. A bare `npm test` running in the main checkout with no worktree registered is not detected —
  that is what `--running` is for, and both skills instruct the caller to pass it.
* One assertion in `install-drift.test.sh` (the package manager missing from `PATH`) is skipped with a
  printed `note` on a host where `npm` shares a directory with `git` or `python3`; it ran here.
* No best-practices folder exists in this repo (`find . -type d -name best-practices` is empty), so the
  rulebook's fallback would have been a web search; the repo's own facts and conventions were used instead.
* `pipeline: false` — no CI workflow was created or touched.

**Effort**: 41 min elapsed (budget 200, written in the turns era — read at the measured rate, it was not
approached) — wt:task-pi-51-reinstall-shared-checkout. No `BUDGET` and no `STALL` line was logged.

---

# Round 2 — the four review findings, against the corrected task file

The task file was rewritten on `main` after round 1 (AC1 and AC3 changed, AC6 is new). This round started by
merging `origin/main` into the branch and re-reading the acceptance criteria from the merged state; two
conflicts were resolved by hand (`docs/SESSION_HANDOFF.md`, keeping both log lines; the task file, keeping
`Status: In Progress`, the real branch name and the PR link).

## F1 — "the installed tree could not be compared" is a class, and it never yields a verdict

Round 1 treated pnpm/yarn/bun as a `warn` and went on to publish a verdict. The task's AC3 asks for the
opposite and names the option: **refuse the verdict for the whole class**. `verify.sh`'s third `case` arm is
now a refusal — `FAIL install drift — … could NOT be compared …`, install-drift's own reason carried
through, no remedy invented, `verify: could not run …`, exit 2 — and it fires for every member of the class:
every lockfile of a package manager `install-drift.sh` cannot read, and every lockfile it can name and not
parse (unparsable, wrong JSON type, unknown shape). The base-attribution side refuses the same way through a
new `elif [[ $BD_RC -ne 0 ]]` branch: a base tree that was never compared is no more evidence than one that
disagrees, so the branch keeps its own red instead of being excused as "inherited".

The cost is stated in the code as a deliberate trade: a pnpm/yarn/bun project can no longer be verified at
all until a comparator exists. `SUPPORTED_PM` is the single declaration that changes when one is added, and
the refusal says so to the reader.

**N1 (reviewer note).** `verify.sh` no longer keeps its own list of lockfile names for the borrow decision:
`install-drift.sh lockfiles` (new subcommand) prints the declaration, `verify.sh` reads it and fails closed
if it comes back empty. A lockfile name added in one place can no longer go missing in the other.

## F2 — liveness is live processes, and the remedy runs verbatim

Registration is not liveness. `live_runs()` reads the working directory of every process of this user
(`/proc/<pid>/cwd` on Linux, `lsof -a -d cwd -w -F pn -u <uid>` elsewhere, measured 0.08 s) and reports only
processes whose cwd is inside the checkout, excluding this run's own ancestors and descendants. A registered
— even locked — worktree with nothing running in it no longer holds the reinstall; it is printed as a note,
so an operator can see the reinstall was a decision and not an oversight. `--running` and "the prober could
not answer" still block.

Order and `--except`: the closing-step bullet was **moved after** the `cleanup-merged.sh` bullet in both
`quickfix` and `run-wave`, and the command passes `--except {the merged branch}`, so the merged PR's own
worktree is never something to wait for. `verify.sh` prints the same form (`reinstall $ROOT --except
$BRANCH`), because the worktree being verified is registered at the very moment the line is printed.

Proof is end to end, not by assertion in isolation: `install-drift.test.sh` §7 extracts the real command out
of each SKILL.md, checks it sits after the cleanup bullet, runs the real `cleanup-merged.sh` and then the
extracted command on a fixture repo with a merged dependency-bump PR whose worktree is still registered.
`verify.test.sh` does the same from the other end: it extracts the `fix:` line from verify.sh's own output
and executes it verbatim, in the world that printed it, then re-runs verify and gets GREEN.

One real bug was found by that end-to-end test and fixed: the `lsof` prober **lists itself**, and it inherits
the script's working directory, so a reinstall run from inside the checkout saw its own prober as a live
agent and deferred forever. The prober's pid is now recorded and excluded, and the two tables are taken in
the order cwds-then-processes so that a pid that has exited in between is not reported as running.

## F3 — reading the worktree list has three answers, not two

`read_worktrees` distinguishes *answer* / *not a git repository* / *unknown* (git absent, `rev-parse`
failing for any other reason, `worktree list` non-zero, decoder non-zero). Unknown defers, with the reason
printed. A plain directory that is not a repository is not an unknown — it reinstalls.

## F4 / AC6 — a deferral is never a silence

`log_deferral` counts the occurrences of its key in `docs/SESSION_HANDOFF.md` **and** in the archive before
and after the write, and only claims "logged … with handoff.sh log" if the count went up. Otherwise it
prints `install-drift: NOT LOGGED — …` on stderr, naming what could not be written and where it looked. A
missing `handoff.sh` next to the script produces the same visible failure. Both skills tell the operator to
carry the deferral into the report when that line appears. Tested on the failure path: an orphan
`install-drift.sh` with no `handoff.sh`, and a read-only `docs/` — with a probe asserting `handoff.sh` exits
0 on a refused write, which is why counting, not the exit code, is what the claim rests on.

## Acceptance criteria → tests (round 2)

| Criterion | Test |
|---|---|
| AC1 — reinstall fires in the real closing-step order, remedy runnable verbatim | `install-drift.test.sh` §7 (both SKILL.md files: bullet order, command extracted and run after the real `cleanup-merged.sh`); `verify.test.sh` "PI-51 F2 — run exactly as printed …" |
| AC2 — no reinstall under a running agent | `install-drift.test.sh` "a live process in a worktree defers …", "`--except` does not silence a live process", "a process in the main checkout defers"; registered-but-idle worktree reinstalls |
| AC3 — the uncompared class never yields a verdict | `verify.test.sh` class loop over the production declaration (exit 2, no verdict, no check executed) + three malformed-lockfile cases + the base-side refusal |
| AC4 — unchanged behaviour where the tree matches | the GREEN/no-lockfile/nothing-installed cases, unchanged from round 1, still pass |
| AC5 — mutation | `install-drift.test.sh` mutation section; `verify.test.sh` "PI-51 AC5 — without that arm the same world is declared GREEN on a tree nobody compared" |
| AC6 — a deferral that could not be logged is visible | `install-drift.test.sh` orphan-script and read-only-`docs/` cases, asserting the `NOT LOGGED` line and the absence of any "logged" claim |

## Checks

```
bash bin/install-drift.test.sh   → ALL PASS (130 assertions)
bash bin/verify.test.sh          → ALL PASS (262 assertions)
full testCommand (13 suites)     → exit 0, 1524 assertions
bash bin/doctor.sh               → 0 error(s), 0 warning(s), 53 task file(s)
```

## Notes and deviations (round 2)

* Files touched outside the task's `**Files**` line: none new — `CLAUDE.md`, `.claude/agents/reviewer.md`
  and both `SKILL.md` files were already in scope in round 1 and carry the new meaning of exit 2.
* The round-1 note "liveness is read from worktrees, processes are not inspected" is **superseded**: it is
  now the other way round, and `--running` remains for what no prober can see (a run whose working directory
  is outside the checkout).
* Still deliberately not covered: the contents of an installed package at the version it declares.
* `pipeline: false` — no workflow created or touched.

**Effort**: not available — no start timestamp — wt:task-pi-51-reinstall-shared-checkout
No `BUDGET` and no `STALL` line was logged this round.

---

# Round 2b — the PR body, and where the rule for it now lives

The only finding left after round 2 was text, and the reviewer is right that it is not a small one: the body
of PR #96 was still round 1's. On a squash merge that body becomes the commit message on the base branch and
stays there, so it was about to publish, permanently, the **opposite** of what was delivered ("processes are
not inspected", when liveness is now read from live processes), two test names that no longer exist, no AC6
row, and a Changes list missing three changed files.

1. **The body was rewritten against the current head** with `gh pr edit 96 --body`: a criteria table with
   one row per criterion including AC6, a Changes section taken from `git diff --stat origin/main...HEAD`
   (one line per file, nothing omitted), and the deviations as they are now — the superseded liveness note
   deleted rather than left standing next to its replacement.
2. **The rule is in the file the agent reads at start**: `shared/implementing-common.md` §9, under *Resume
   report format*, next to the rule about the scope of a resumed branch — a resumed branch rewrites the PR
   body to the current head, not only the report and the review comment, with the reason (the squash-merge
   commit message) stated.
3. **The lesson of method** was also written as a line in `.claude/agents/shared/lessons.md` — and that was
   wrong; round 3 deletes it, see below.

## Handoff

`origin/main` was merged again for this round (the lessons file had been rewritten meanwhile) and it
conflicted on `docs/SESSION_HANDOFF.md` and `docs/SESSION_HANDOFF_ARCHIVE.md` — the defect scheduled as
PI-52. Resolved by taking `main`'s side for both files, so **this branch no longer touches the diary at
all**; the lines round 2 had written there are carried here instead, for whoever merges to apply from the
main checkout:

Five commands, to be replayed **with the tool** from the main checkout after the merge — not reworded, not
written by hand (§6). `handoff.sh`'s own `one_line()` collapses the wrapping below into a single record:

```
bash bin/handoff.sh log "PI-51 round 2 pushed on #96 — F1 verdict refused for the whole uncompared class (verify.sh reads the lockfile names from install-drift.sh), F2 liveness from live processes with --except and the reinstall moved after cleanup-merged.sh, F3 three-state worktree read, F4/AC6 NOT LOGGED on stderr — 130 + 262 assertions, full suite 1524, doctor clean"
bash bin/handoff.sh log "PI-51 round 2b — PR body rewritten to the head; the rule for it is in implementing-common.md §9, Resume report format"
bash bin/handoff.sh log "PI-51 round 3 — F6 the duplicated lesson line deleted (the rule at implementing-common.md §9 is the promotion), F7 the Effort lines put in the prescribed shape, F8 this section put in command form; no code, test or skill touched"
bash bin/handoff.sh fact "lsof lists itself and inherits the caller's working directory: any liveness probe that reads process cwds must record and exclude its own prober pid, or a check run from inside the directory it guards always sees a phantom process there"
bash bin/handoff.sh fact "a worktree being registered is not an agent running: liveness is a live process whose working directory is inside the checkout; registered-but-idle worktrees are reported, never waited for"
```

**Effort**: not available — no start timestamp — wt:task-pi-51-reinstall-shared-checkout
No `BUDGET` and no `STALL` line was logged this round.

---

# Round 3 — three text edits in two files

Findings, one line each (Resume report format), all rounds:

* F1 — closed (round 2, `3bed20c`) · F2 — closed (round 2, `3bed20c`) · F3 — closed (round 2, `3bed20c`)
* F4 / AC6 — closed (round 2, `3bed20c`) · N1 — closed (round 2, `3bed20c`) · F5 — closed (round 2b, `c4b3eca`)
* **F6 — closed**: the lesson line this branch added to `.claude/agents/shared/lessons.md` is deleted. The
  file's own header forbids a lesson whose rule already exists where an agent reads it, and the rule was
  written in the same commit, at `shared/implementing-common.md` §9 — so the line was born already promoted.
  Writing the rule **is** the promotion; the §9 rule stays and nothing else changes. `git diff origin/main --
  .claude/agents/shared/lessons.md` is now empty (12 lessons, the file's 300-byte guard prints nothing), and
  the "the same, as a method lesson" bullet is gone from the PR body.
* **F7 — closed**: both `Effort:` lines carried a tilde, which §7 defines as an estimate. The clock was not
  taken at the start of rounds 2 and 2b, so both now read exactly
  `Effort: not available — no start timestamp — wt:task-pi-51-reinstall-shared-checkout`; no other quantity
  is substituted for the missing minutes. Round 1's line was already correct and is untouched. This round's
  own line reads the same way, and for the same reason: the round resumed after a context compaction with no
  clock taken at its start, so there is no start timestamp to subtract from and no number is invented.
* **F8 — closed**: the `## Handoff` section is now five `handoff.sh log|fact "…"` commands in a fenced
  block, to be replayed with the tool from the main checkout; the round-2b line's text was corrected where
  it referred to the lesson this round deletes. The diary files stay out of the diff.

No code, no test and no skill was touched. Checks re-run on this head: full `testCommand`, 13 suites, exit 0,
1524 assertions; `doctor.sh` 0 errors, 0 warnings. The PR body was realigned to this head, which is the §9
rule this branch wrote.

**Effort**: not available — no start timestamp — wt:task-pi-51-reinstall-shared-checkout
No `BUDGET` and no `STALL` line was logged this round.
