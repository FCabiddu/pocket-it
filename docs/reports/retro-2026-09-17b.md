# Retro 2026-09-17b — signals-2026-09-17b

**Trigger**: one signal from `bin/retro-due.sh`, verbatim — `PI-51 — needs-work-cause — 2026-09-17 PI-51 PR #96 needs work (delta 2) — lessons.md keeps a copy of the rule its own commit wrote — cause: other: the fix round added a shared-file line that file's rules forbid`. Third targeted pass of the day (`retro-2026-09-16.md`, `retro-2026-09-17.md`). Their findings are not re-derived and their numbers not re-counted; only PR #96 was read.

## Evidence

- **PR #96** (PI-51, DevOps, `Risk: high`, Estimate M / Budget 200): merged 2026-09-17T21:20Z, labels `approved` + `Auto-merge`, **7 comments = 4 rounds** (round 1 NEEDS WORK `first-round`, 4 findings + 1 note · round 2 delta NEEDS WORK `other: PR body never updated` · round 2b NEEDS WORK, 3 findings incl. **F6**, the signal · round 3 delta APPROVED). Round 3 touched **no code, no test, no skill**: one deletion in `lessons.md` and report text.
- **F6 as the reviewer measured it**: round 2b had written the resume-report rule into `shared/implementing-common.md` §9 *and* a copy of it into `shared/lessons.md`. Closed by deleting the copy — `git diff origin/main...HEAD -- .claude/agents/shared/lessons.md` empty, 13 lessons on both sides.
- **Where the rule was reachable, verified here, not assumed** (`grep -rn "lessons" .claude/agents/ .claude/skills/ CLAUDE.md`): the *writing* rules for `lessons.md` live in three places, **none of which an implementing agent reads** — the file's own header (read only by whoever opens the file, not by whoever appends to it), `retro.md:57`, and `CLAUDE.md:90` (the orchestrator's). What developer/qa/reviewer do read, `implementing-common.md:38` (§3, "Zeroth"), describes the file as a **read**: "what held is already a rule in the files below" — a hint, never a prohibition, and it says nothing about when a line there is owed. `developer.md` and `reviewer.md` never mention the file at all. §7b, which does name `shared/lessons.md` among the artefacts it binds, binds only the project-isolation dimension.
- So the defect is a **rulebook gap, not a developer defect**: the agent had no way to know, at the point where it worked, that writing the rule into a start-read file *is* the promotion.
- Counters since the 2026-09-17 mark: causes **3** on one PR (`first-round`, `other` ×2), repeat cause **0** (each round a different cause, all closed on the next). BUDGET/STALL: **0 new** (latest 2026-09-13/14). Isolation blocks 0, `general-purpose` launches 0. Facts **64** / cap 100 — hygiene threshold (85) not reached, nothing moved or deleted. `usage-report.py --days 14 .`: `no transcripts` for this project's resolved path (the work runs from another session's cwd) — same limitation as the two previous passes; the handoff log carries the signal.

## Trend vs the previous retro (2026-09-17)

Cause lines in the window: 1 → 3, all three on one high-risk PR and all three closed on the following round — rework rate 1 PR / 1 PR, but no cause repeated and none reopened. `example-not-class` 0 → 0, `base-moved` 0 → 0, `false-coverage-evidence` 1 → 0 (the rule written last pass held: round 1's own `false-coverage` class did not recur on #96). BUDGET/STALL, isolation blocks, `general-purpose`: 0 → 0. Facts 59 → 64. New class this pass: **a fix round violating the rules of the shared file it writes into** — the first of its kind.

## Written this pass

- **`shared/implementing-common.md` §3, one bullet** — read at Step 0 by developer, qa-engineer, reviewer, planner, immediately under the bullet that sends them to `lessons.md`: *you read that file; you do not add to it when you close a defect* — the rule goes where agents read it at start, and writing it there **is** the promotion; a lesson is only for a destination not yet decided or not yet reachable, and that line goes in the report for `retro`.
- **`reviewer.md` checklist, one line** — **Governed files**: when the diff adds lines to a file whose own header states what may be written in it (`shared/lessons.md`, a `best-practices/` file, `docs/SESSION_HANDOFF.md`, an agent template), read that header and judge the added lines against it, not against the finding they answer. Worth its cost: the reviewer already reads the diff's file list, the class generalises past `lessons.md`, and the only reason this defect was caught at all was one reviewer reading carefully rather than any written check.
- **No lesson in `lessons.md`, deliberately.** Writing the rule at a start-read destination is the promotion; a line here would be the very defect the signal reports. File untouched — `git diff --name-only` confirms 0 changes to it; still 13 lessons, 33/40 lines.
- **Nothing promoted, nothing removed** from `lessons.md`: none of the 13 is about this class, and none gained a second independent occurrence in this window.

## PRs

- Rules + report: **one** PR, merged — this project *is* pocket-it, so the method layer and the project layer share a repo and a tree; two sequential PRs over the same files would only manufacture a conflict. `doctor.sh`: 0 errors, 0 warnings, 55 task files.

## Not actioned

- **N2 from round 3** ("12 lessons" where the count is now 13, `docs/reports/PI-51-2026-09-17.md:295`) — a stale parenthetical in a merged report, explicitly declined by the reviewer as not worth a round; the load-bearing claim next to it is true. One-off, no rule follows from it.
- **`handoff.sh log` exits 0 on a refused write** (measured twice during PR #96, F4 and round 2b) — real and already routed: the reviewer sent it to the orchestrator as its own item, and `install-drift.sh` now works around it by counting the key before and after. It is a code defect in `bin/handoff.sh`, not a rule gap, so it belongs on the board rather than in a text-only retro PR. Not a one-off and not dropped: named here so the next pass sees it if no task has been opened for it.
- **Four rounds on one PR** — read for a pattern and none found: the causes are all distinct and each round closed what the previous one opened, on a `Risk: high` task whose own subject was a guard reaching verdicts without evidence. No round-limit or grouping rule follows from a single PR with no repeated cause.

## Nothing waits on a person

Every rule from this pass is written and merged in the same run. No point of this retro needed a business decision, money, credentials, client data or a physical check.
