# Retro 2026-09-18 — signals-2026-09-18

**Trigger**: 2 signals from `bin/retro-due.sh`, verbatim, both on PI-47 / PR #94 — `needs-work-cause … cause: other: a task id cited in shared/implementing-common.md as an existing mechanism was read as merged and asserted as fact` and `cause: example-not-class`. Re-derived here, not taken on trust. Earlier passes (`retro-2026-09-16.md`, `-09-17.md`, `-09-17b.md`) are not re-counted.

## Evidence

- **PR #94** (PI-47, DevOps, `Risk: low`, Estimate S / Budget 120): merged 2026-09-18T04:42Z, labels `approved` + `Auto-merge`, 3 comments = **2 review rounds** (round 1 NEEDS WORK, 1 finding + 2 notes · round 2 `Mode: delta` APPROVED, merge: orchestrator). Code and tests were certified correct in round 1; **the entire round 2 was text**.
- **Effort, from the report's own line**: 13 + 16 + 34 = **63 min against a 120-min budget** — inside budget. No estimate correction is owed.
- **BUDGET/STALL in the window**: **0 new** (`handoff.sh grep "BUDGET|STALL"` → latest 2026-09-13/14). `usage-report.py --days 14 .` → `no transcripts` for this repo's resolved path, a fourth consecutive pass: the work runs from another session's cwd. Both already carry a task — `tasks/PI-58` — opened by the previous pass; nothing added here.
- **Facts: 67 / cap 100.** Hygiene threshold (85) not reached: nothing moved, nothing deleted.
- Isolation blocks **0**, `general-purpose` launches **0**.

### The class, measured across the PR's whole life — three unexecuted claims, two roles

1. **Developer, round 1** — report and PR body: *"PI-52 (merged since round 1) now makes that the rule"*. `PI-52` was `Status: Todo`, no branch, no PR. Inferred from `implementing-common.md:111`, which cites the id in the present tense.
2. **Reviewer, round 1, note (b)** — asked for a code comment to be rewritten, giving prose direction and no measured text.
3. **Developer, round 2** — rewrote that comment: *"the cut removes the whole `pm)` body"*. Derived by reading the `awk`, not by running it. Three lines survive the cut, and they are load-bearing.

Round 2 found (3) only because the reviewer ran the cut that time. **The one round the text was executed is the round it was right** — that is the whole finding: an unexecuted claim keeps travelling until someone runs something, and a correction derived by reading inherits the defect it corrects.

## Trend vs the previous retro (2026-09-17b)

Cause lines 3 → **2**; rounds on the triggering PR 4 → **2**; rework rate 1/1 PR in both windows. `example-not-class` 0 → **1** (new this pass). `other` 2 → 1. BUDGET/STALL 0 → 0, isolation blocks 0 → 0, `general-purpose` 0 → 0. Facts 64 → 67. Lessons 13 → 12. Open rulebook tasks 3 → 5.

## Written this pass

- **`shared/implementing-common.md` §9, the "absences you did not measure" bullet — widened, not duplicated.** It said *a conclusion that there is nothing to change is a measurement, not a reading*, and covered only that shape. It now binds **any** assertion of fact handed over — the state of a task, branch or PR, what code does, what a mutation removes, a number, a size — names the artefacts (report, code comment, **PR body**, shared memory), requires the command be named beside the claim, and states the cheapest case outright: *a task id or a PR number is a state you check on the board, never one you infer from the prose that cites it*. The two open tasks it cites are marked **not built yet**, in the sentence, so it does not commit the very defect it describes.
- **`implementation-planner.md` Step 4, under the criteria block.** A criterion names its target by content, never by a line number or a total another open task can move. Measured live in this pass: `tasks/PI-56` AC2 said *"line 31 is deleted"* and AC4 *"the file holds 12 lessons (13 before)"*; deleting one unrelated line above it made both false, and a correct implementation would have gone red. Fixed in PI-56 (content anchor + a delta the change owns) and turned into the planner rule so the next board does not carry one.
- **`tasks/PI-59` (new)** — the mechanical half: a task or PR state claimed in a committed report or a PR body is read off the board and disagreement is red; a bare cross-reference stays silent, and the ambiguous case chooses silence (AC3) because a check that fires on ordinary mentions is a check that gets switched off. Cited by the §9 rule.

## Lessons

- **Removed 1, promoted into §9** — `2026-09-11 · WHERE …§7 · WHEN a number goes into a report or a criterion · DO produce it with the tool that owns it … and name that tool`. Its "produce it with the tool that owns it, and name that tool" clause and its 25016-against-229 evidence are folded verbatim into the widened §9 bullet. Promoted to §9 rather than to its declared §7 because the concept already had a home there and **one home per concept** wins over the declared `WHERE`; the file's own rule (*a lesson whose rule already exists somewhere an agent reads is deleted*) applies either way. 13 → 12 lessons, cap check prints nothing.
- **New: none.** Every rule from this pass landed in a file read at Step 0, so a lesson line would be a duplicate paid at every launch.

## Already covered — no second rule written

Verified against the task files rather than taken from the launch prompt:

- `tasks/PI-55` — forward references in start-read files must not read as existing mechanisms. Covers the **source** of claim (1). Its AC1 scopes itself to a search, not to `:111`. Sufficient; not reopened.
- `tasks/PI-57` — `reviewer.md` Step 4's "execute your correction before proposing it" must bind notes and replacement text, not only findings. That **is** claim (2), and is the reviewer's own stated fix for the `example-not-class` signal. Sufficient; signal 2 needs nothing else.
- `tasks/PI-56` — promotes the "an exclusion is an assertion" lesson into §7. Same family as the §9 widening but a different clause (completeness of a classification vs. execution of a claim); AC3 already tells its developer to merge rather than sit beside an existing bullet. Not duplicated — only its stale anchors repaired.
- `tasks/PI-58` — BUDGET/STALL derived from measurement with a red gate. Covers both instrumentation gaps this pass re-measured.

## Not actioned

- Reviewer round 2's non-blocking finding (the `MUT_NOFIX` comment clause, with measured replacement text) was left for "a later PR that touches the file". It is a correct call — the text is a comment, the conclusions hanging off it are true — but it now has no owner. Recorded here so the next PR touching `bin/verify.test.sh` picks it up; not worth a task of its own.
- The `Effort` readability point (`29 total` on the first line while the task total was 63) — a single formatting observation, closed inside the same review, no second occurrence.

Nothing in this pass needs a decision from the user: no business call, no money, no credentials, no client data, no physical check. Everything above is written and merged in this same run.
