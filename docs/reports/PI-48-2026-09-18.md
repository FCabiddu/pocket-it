# PI-48 — cleanup-merged.test.sh's self-mutations go through the shared gate

**What & why.** The file built its patched copies with a bare `sed` and concluded "applied" from its own output; a mutation that silently fails to apply then fails a *downstream* assertion — a test failure naming the wrong test. Every self-mutating block now goes through `mut_site()`, which sources `bin/self-mutation-gate.sh`. AC2 asked for the gate itself, not a copy: it was **moved** out of `bin/doctor.test.sh` and both suites source the one file.

**Provenance — is the extracted helper the gate on `main` today?** (asked first, before any change) `git merge origin/main`: clean. `git ls-tree origin/main bin/`: main has no gate file; the gate is `bin/doctor.test.sh` 73–132. `git show origin/main:bin/doctor.test.sh` diffed against `bin/self-mutation-gate.sh`, comments and blanks stripped: every difference is an **addition** (ledger, `sed_rhs`, `occur`, tail check, pristine 3rd arg) — nothing landed was dropped. PI-63 changed no gate definition (`git show --stat` on its commits: no `bin/` file). One reconciliation was needed: the WIP commit had *replaced* the landed python-heredoc check with a tail check. Restored — the two `MWEOF` bodies now `diff` identical, 20 lines — and made conditional on the pristine source opening such a heredoc, since `cleanup-merged.sh` has none and the landed check would otherwise refuse every mutant of it, valid ones included. The tail check stays as an *added* layer. Current `main` (68c1511) re-merged at the end; again no `bin/` change.

**Baselines, measured before changing anything.** `doctor.test.sh` at `e997e07`: 219 ok / 0 FAIL; on the merged tree: 219 ok / 0 FAIL, ok-line lists identical (`diff`). `cleanup-merged.test.sh` before: 206 ok / 0 FAIL.

**A defect in the landed gate, found by executing AC3 instead of tabulating it.** `src_anchors`' `substr` mode counts matching *lines* (`grep -cF`) — right for a range address, wrong for a plain `s///`: two occurrences on **one** line counted as one and sailed through. Three AC3 rows went red on it. Added an `occur` mode (occurrences, `grep -oF | grep -c ''`), the unit a substitution works in; `line` and `substr` untouched, so `doctor.test.sh` is unaffected.

**AC → tests** — `bash bin/cleanup-merged.test.sh`: **253 ok / 0 FAIL**, exit 0.
- **AC1** mechanical, not a habit: `run_mutant()` refuses any copy the gate's ledger does not list (99, nothing on stdout); a refusal reports `self-mutation refused at site {s} …` quoting `MUT_WHY`. 8 rows.
- **AC2** `source self-mutation-gate.sh`; no gate logic restated in either test file.
- **AC3 — 42 rows, every one RUN, none read.** Per site `damage()` writes a damaged *source*, `mut_site` is invoked on it and must refuse, and `raw_sed()` is run to show what the ungated substitution would have produced. `pi31`, `pi44a`, `pi44b` (`occur`) × {anchor-removed, anchor-doubled, anchor-doubled-inline, marker-in-source, anchor-annotated} = 11 rows each; `pi38` (`line`) 9 — `anchor-doubled-inline` is not a separate case for a whole-line anchor (doubling it inline *is* the annotated case, `/^…$/` stops matching) and that case is run, as `anchor-annotated`, with the opposite outcome. 11+11+11+9 = 42.
- **AC4 — four sides, none derivable from another.** (1) filesystem: `mutant_files()` → 4 copies by content; (2) the gate's **runtime** ledger → 4 still on disk; equal, `ungated_copies()` empty, and a sibling builds one copy deliberately ungated and is named by the same census. (3) static text: no line redirects `$SCRIPT`/`$SCRIPT_SRC` into a path outside `mut_site` → 0, sibling appends one and finds exactly it. (4) declared vs executed: 4 declared in `mut_decl`, 4 recorded as run against the captured source. **The census's one filter, measured not reasoned (§7):** unfiltered `find` over both roots = **1109** files = 1104 below the size floor (largest shares 9 of 373 lines, none near the 187 required) + 5 above = 4 mutants (all in the ledger) + the pristine capture, skipped by name; census with and without the floor: identical, 4 files. Measured with a throwaway instrumented copy of the suite, deleted after, itself 253 ok / 0 FAIL.
- **AC5** 253 ok (from 206), 0 FAIL; `doctor.test.sh` still 219 ok / 0 FAIL, identical ok-line list.

**The chained `testCommand` exits 1 — not from this branch.** `bin/handoff.test.sh` fails one AC8 row about `reviewer.md`. Pre-existing: `git archive origin/main | tar -x` into a clean dir, `bash bin/handoff.test.sh` → exit 1 there too. This branch touches no file that check reads; every other suite in the chain is green.

**PI-64 untouched and not moved.** `mirror_run`'s `rm -rf`-as-last-command defect is exactly where it was; this change never enters that function.

**Deviations.** Outside the task's original `**Files**`: `bin/self-mutation-gate.sh` (new — AC2's "move it" *is* the work) and `bin/doctor.test.sh` (103-line gate block → one `source` line, plus explicit pristine arguments). Both are the criterion, not scope creep. The task's title says "two" blocks; there are **four**, recorded in the task file with the command that found them.

**How `**Files**` was re-derived:** from the branch, not from memory — `git diff --name-only $(git merge-base origin/main HEAD) HEAD` gave the three `bin/` files; the task file itself is added because PI-65 AC4 excludes `docs/reports/**` and `docs/handoff/**` by name but not it.

Effort: not available — no start timestamp — wt:task-pi-48-cleanup-test-reuses-gate
