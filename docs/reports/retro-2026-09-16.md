# Retro 2026-09-16 — signals-2026-09-16

**Trigger**: `bin/retro-due.sh` signal, verbatim — `PI-44 — needs-work-cause — 2026-09-16 PI-44 PR #84 needs work — prunable branch kept silently, never announced/counted — cause: example-not-class`. Scope narrowed by the script to PI-44 only (per the `Signals:` convention the broad `gh pr list --search` step was skipped).

## Evidence
- `tasks/PI-44-cleanup-summary-undercounts-and-invents-a-cause.md` → PR #84, 1 needs-work round, label on at read time. Rework rate in scope: 1/1.
- Reviewer comment on #84, read in full: `verify.sh` GREEN; four `keep()` call sites genuinely fixed; the AC1 test proven non-vacuous against the reviewer's own independent 5th mutation. Blocking finding at `bin/cleanup-merged.sh:339-342` — the `prunable` branch of the main loop keeps a branch in two **silent** variants (protected branch without `--all`; `decide()` returning keep with its stdout sent to `/dev/null`), reproduced for real on minimal repos: branch still resolvable, summary `0 kept`.
- `handoff.sh grep "BUDGET|STALL"`: 3 lines, all older than the last retro-mark (2026-09-15) — no new estimation or stall pattern. `usage-report.py --days 14`: no transcripts under this project's resolved path (this work runs from another session cwd) — not blocking at this scope, the handoff log carries the signal. Facts 54 → 56 (85 hygiene threshold not reached, nothing moved or deleted); `shared/lessons.md` 40/40, at cap.

## Trend vs the previous retro (2026-09-15, PRs #78/#79)
Rework rate in scope 1/1 both runs (single-PR scopes, not comparable as a rate). Causes since the 2026-09-15 mark: 5 needs-work lines — 4 `first-round`, 1 `example-not-class`; zero `base-moved` (the previous retro's pattern, closed mechanically by PI-41/#83 — the fix held). BUDGET/STALL: 0 new. Isolation blocks: 0. `general-purpose` launches: 0.

## Pattern found (1)
**`example-not-class`, 2nd occurrence since 2026-09-14** (1st: PI-35 PR #66 delta 3, a reviewer's hand-copied six-of-seven forms; 2nd: PI-44 PR #84, a task's Notes enumerating four-of-five call sites). The novel half, and the reason the existing rules did not save it: `implementing-common.md:170/:173` already tell an agent to derive the test from the declaration and to regenerate a handed-down list from its producer — and both were honoured. They failed because **the producer here is the program's own announcement**. AC1 was phrased as `summary count == number of printed "kept" lines`; a faithfully derived test from that phrasing is green on a path that keeps a branch while printing nothing (`0 == 0`), and a grep of `echo "kept` — the only regeneration available — cannot list a site that prints nothing. Silence was a class member no announcement-shaped check could reach. The task's `## Notes` (orchestrator-written) stated the enumeration next to a correctly effect-shaped damage line; the enumeration is what got fixed.

## Rules written
- Project facts (2): the `keep()` invariant for `cleanup-merged.sh` with its two silent variants and the measurement that actually decides it (`git for-each-ref refs/heads` before/after minus the branches really deleted — never printed lines vs summary); and that an enumeration in `## Notes` outranks an invariant in the ACs, so Notes carry declared starting points only and the criterion states the observable effect.
- Method lessons: 1 new provisional (a class visible only through what the program announces is restated as an effect and measured outside the program's output, with "prints nothing" as a case) and 1 confirmed (the 2026-09-15 "list of accepted forms" lesson, second independent occurrence, extended to call-site lists and the effect anchor).

## PRs
- https://github.com/FCabiddu/pocket-it/pull/PROJECT_PR — merged (2 facts + this report)
- https://github.com/FCabiddu/pocket-it/pull/LESSONS_PR — merged (lessons: 1 new, 1 confirmed, 1 removed)

## Lessons
- New (provisional): `2026-09-16 · WHEN a class of cases is only visible through what the program announces …` — evidence: PI-44 #84 round 1.
- Confirmed: `2026-09-15 · WHEN a finding or a task hands the implementer a list of accepted forms or of call sites …` — 2nd occurrence (PI-35 #66 delta 3, PI-44 #84).
- Removed: `2026-09-15 · WHEN a finding enumerates conditions (a), (b), (c)… · DO close it with one report line per letter` — already promoted verbatim, evidence included, to `shared/implementing-common.md` §6 ("A finding that enumerates conditions (a), (b), (c)… is not one finding but one per letter"); per this file's own rule a promoted lesson is deleted from here, and the cap (40) needed the line.

## Proposed changes to pocket-it (human step)
1. `shared/implementing-common.md`, appending to the existing bullet "**A list handed to you — in the task, in a finding, in a prompt — is an input, not the class.**": `When the only producer you can regenerate from is the program's own output — a printed line, a log entry, a counter — that regeneration is blind by construction to the member that produces nothing: anchor the list, and the test, to the effect the class leaves behind (the objects still standing after the run, compared before and after) and write "prints nothing" into the case table as its own row.`
2. `shared/implementing-common.md`, same section: `An acceptance criterion must never be an equality between two things the code under test prints: both sides go to zero together on a silent path, and the derived test is green over the defect.`
3. `.claude/agents/reviewer.md:21`, extending the `example-not-class` gloss (today: "a guard closed for two command shapes, open for a third"): `— including the silent member: a path that produces the damage while announcing nothing, which neither the enumeration nor a grep of the announcement can list.`
4. `implementation-planner.md` / task-writing rules: `Any list of call sites, files or line numbers in ## Notes is labelled "Starting points only, not the boundary of the work" on the same line, and the criterion it illustrates is stated in the ACs as the observable effect, never as the code's own report of it.`

## Not actioned
- PI-45 (#85) `first-round` and the other three `first-round` lines since the mark: first reviews, no prior round to explain, no pattern.
- PI-44's own round-2 fix (the fifth call site, its test, and the `kept_equals_summary` rework): live task work owned by the developer/reviewer lane, not a rulebook change.

## Exception
None. No business, money, credential, client-data or physical-check point in this scope: every rule, fact and lesson from this pass is written and merged in the same run; only the four proposed template lines above wait for the deliberate human promotion step the pipeline defines.
