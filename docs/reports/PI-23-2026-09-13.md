# PI-23 — report, giro 3

## Cosa non bastava del giro 2 (causa: example-not-class)

Il delta review su 627885c..b0d2b7b ha trovato due punti in cui la correzione del giro 2
trattava un esempio invece della classe:

1. `design-compass.md:105` — il limite sull'offset iniziale (`X` in
   `translateY(calc(100% + Xem))`) era scritto come regola generale ("X almeno il padding
   appena aggiunto") ma misurato in un solo punto (`X = .6em` contro `.25em` di un solo lato),
   lontano dal limite reale.
2. `design-compass.md:49` — il controllo sui pixel neutralizzava solo `overflow` e
   `clip-path`, elencati come se fossero l'intera classe di proprietà che ritagliano il
   rendering, mentre ne restano fuori `contain`, `content-visibility`, `mask*`,
   `clip: rect()`, `overflow-clip-margin`.

## Misure di questo giro (Playwright/Chromium, pixel-only)

Script in `/private/tmp/.../scratchpad/pi23/{offset,clippers}.mjs` (ambiente locale, non
nel repo — coerente con "questo repo è pubblico, niente artefatti di misura specifici di
un progetto committati").

**offset.mjs** — sweep di `X` (`.25em .35em .5em .6em`) su 5 font
(Georgia, Baskerville, Didot, Helvetica, Futura), testo `ÈÅÖ gjpqy Ùç`, `padding: .25em 0`
(totale `.5em`) sul contenitore mascherante:

```
Georgia     X=0.25em leaked=true   X=0.35em leaked=true   X=0.5em leaked=false  X=0.6em leaked=false
Baskerville X=0.25em leaked=true   X=0.35em leaked=true   X=0.5em leaked=false  X=0.6em leaked=false
Didot       X=0.25em leaked=true   X=0.35em leaked=true   X=0.5em leaked=false  X=0.6em leaked=false
Helvetica   X=0.25em leaked=true   X=0.35em leaked=true   X=0.5em leaked=false  X=0.6em leaked=false
Futura      X=0.25em leaked=true   X=0.35em leaked=true   X=0.5em leaked=false  X=0.6em leaked=false
```

Conferma esatta il finding: solo da `X = .5em` (= somma padding sopra+sotto) la riga resta
nascosta su tutti e 5 i font; una quota di un solo lato (`.25em`, `.35em`) lascia vedere
l'inchiostro ovunque. (Bug nel mio primo tentativo di script: avevo messo `padding` in `em`
relativo al `font-size` ereditato dal body — 16px — mentre `X` era in `em` relativo al
font-size del testo — 80px — quindi non allineati; corretto dando allo stesso contenitore
il `font-size` del testo, così `padding` e `X` condividono la base.)

**clippers.mjs** — per ciascuna proprietà della classe, tre scatti (spedito / solo
`overflow:visible;clip-path:none` / classe intera neutralizzata), diff in pixel
(pixelmatch, soglia 0.15):

```
property                     shipped_ink  old-check(overflow+clip-path only)   full-check(whole class)
overflow:hidden               2009        no (2890px diff)                     YES (2890px more ink)
overflow:clip                 2009        no (2890px diff)                     YES (2890px more ink)
-webkit-line-clamp             796        no (2890px diff)                     YES (2851px more ink)
clip-path:inset               6676        no (105px diff)                      YES (105px more ink)
clip:rect (positioned)        1861        YES (misses the cut)                 YES (2960px more ink)
contain:paint                 2009        YES (misses the cut)                 YES (2890px more ink)
contain:strict                2009        YES (misses the cut)                 YES (1994px more ink)
content-visibility:hidden        0        YES (misses the cut)                 YES (4168px more ink)
mask-image                    4500        YES (misses the cut)                 YES (1413px more ink)
overflow-clip-margin          2009        no (2890px diff)                     YES (2890px more ink)
```

5 proprietà della classe (`clip:rect`, `contain:paint`, `contain:strict`,
`content-visibility:hidden`, `mask-image`) fanno dichiarare "pulito" (diff < 30px, sotto
soglia rumore) un testo tagliato dal controllo che neutralizza solo `overflow`/`clip-path`;
la classe intera lo scopre sempre (da 1.4k a 4.2k px di inchiostro in più). Mutazione
richiesta dal coordinatore: per ognuna delle proprietà, un caso concreto dove senza
neutralizzazione della classe intera il taglio resta nascosto — le 5 righe "YES (misses the
cut)" sono esattamente quei casi.

## Cosa è cambiato in `.claude/agents/shared/design-compass.md`

1. **Riga 49 (no-clipped-glyphs)**: riscritta per nominare la classe per nome prima degli
   esempi — ogni valore di `overflow` che ritaglia, `overflow-clip-margin`, `clip-path` a
   riposo, `clip: rect()` su elementi posizionati, `contain: paint|content|strict`,
   `content-visibility: auto|hidden`, `mask`/`mask-image`/`-webkit-mask-*`/`mask-border` —
   e a nominare tutti e quattro i lati da cui l'inchiostro può uscire (sotto, sopra,
   sinistra, destra), non solo verticale. Aggiunta la frase esplicita che neutralizzare solo
   `overflow` e `clip-path` non è una versione parziale del controllo ma un controllo
   diverso e sbagliato, con i 5 casi misurati.
2. **Riga 105 (tecnica #1, offset iniziale)**: tolto "X almeno il padding appena aggiunto"
   (misurato su un solo valore, un solo lato); sostituito con "X almeno la somma di
   padding sopra e sotto", misurato sui 4 valori di sweep sopra, con la raccomandazione di
   legare i due valori con una custom property (`--line-pad`) invece di due numeri che
   possono divergere.

## Criteri di accettazione → copertura (dai giri precedenti, invariata su AC1/AC3/AC5)

- AC2 — tecnica riga per riga: il rimedio (padding due lati + margine negativo su un lato)
  resta quello del giro 2; corretto solo il limite dell'offset iniziale che lo accompagna.
- AC4 — regola esplicita nessun glifo tagliato: ora copre la classe intera dei
  ritagliatori e tutti e quattro i lati, non un elenco parziale.

## Deviazioni

Nessuna dallo scope file (`.claude/agents/shared/design-compass.md`). Script di misura
tenuti fuori dal repo (scratchpad locale), coerente con "questo repo è pubblico, niente
artefatti di misura specifici di un progetto".

## Checks

Nessun lint/build/test si applica a un file markdown di prosa in questo repo. Entrambi gli
script di misura girati fino in fondo, output sopra.

## Conflitto di merge risolto

`git merge origin/main` ha prodotto conflitto in `docs/SESSION_HANDOFF.md` e
`docs/SESSION_HANDOFF_ARCHIVE.md` (5 PR mergiate su main nel frattempo). Risolto per
unione: tutti i fatti di entrambi i rami restano (9 comuni + 3 propri del branch + 9 di
main); il log unisce le 17 voci 2026-09-13 di main, le 4 voci PI-25 e le 4 voci PI-23 di
questo branch (aggiunta anche la voce del delta review mai loggata), poi le 13 voci comuni
più recenti fino al cap di 40; l'archivio prende la versione di main, superset di quella
del branch (nessuna voce persa, verificato: le voci non riportate nel log finale erano già
in `arch_main.md`).

## Turni / budget

Round 1+2+3 combinati: budget 120 (S). Nessun `BUDGET`/`STALL` da loggare in questo giro
(progressione con commit + misure verdi).
